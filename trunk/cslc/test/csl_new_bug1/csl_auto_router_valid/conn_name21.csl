//oanab


csl_bitrange br(52);

csl_unit a {
  csl_port p_b(input,1);
  a () {
  }
};

csl_unit b {
  a a0;
  csl_port p_a(input,br);
  b () {}
};

csl_unit c {
  csl_signal s_a(50);
  b b0;
  c () {
    s_a.connect_by_name(b0.p_a[49:0]);  // sig---p.ps (PC)
  }
};

csl_unit d {
  c c0;
  csl_port p_c(input,80);
  csl_signal s_b(52);
  a a(.p_b(p_c[27:27]));  // f2a : port---p.ps
  d () {
    c0.b0.a0.p_b.connect_by_name(s_b[1]);  // port---sig.ps (CP)
  }
};
