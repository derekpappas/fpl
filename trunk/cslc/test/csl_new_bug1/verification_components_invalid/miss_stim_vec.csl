//Monica
// 13 november 2007
// invalid test ---no stim  vector

csl_unit dut{
  csl_port stim(input), exp(output);
  csl_port clk(input);
  dut(){
    clk.set_attr(clock);
  }
};
csl_vector exp{
  exp(){
    set_unit_name(dut);
    set_direction(output);
  }
};
csl_testbench tb{
  csl_signal clk(reg);
  dut dut;
  tb(){
    clk.set_attr(clock);
    add_logic(clock, clk, 10, ns);
  }
};
