//Monica
// 13 november 2007
// invalid test ---no stim and exp vectors

csl_unit dut{
  csl_port stim(input), exp(output);
  csl_port clk(input);
  dut(){
    clk.set_attr(clock);
  }
};
csl_testbench tb{
  csl_signal clk(reg);
  dut dut;
  tb(){
    clk.set_attr(clock);
    add_logic(clock, clk, 10, ns);
  }
};
