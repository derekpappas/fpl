//is not legal to have the same name like parameter, an error for duplicate name must be thrown

csl_register_file rf1{
  rf1(){
   set_rd_addr_name("something");
   set_wr_en_name("something");
   set_rd_en_name("something");

   set_valid_name("");
  }
};