//csl_isa_field_csl_isa_field_object_name.csl
//20 August 2007
//danielt, andreim

csl_enum e_op{ 
  OR, 
    XOR, 
    AND
};
csl_isa_field f_op{ 
  f_op(){ 
    set_width(2); 
    set_enum(e_op); 
    set_type(opcode); 
  }
};

csl_unit top {
};
