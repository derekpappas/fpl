//isa_field_obj_name.set_offset(num_expr);
csl_enum e_op{
   ADD,
   MOV,
   SUB
}; 

csl_isa_field f_op{
  f_op(){
    set_width(2);
    set_enum(e_op);
    set_type(opcode);
  }
};
csl_isa_field f_alu{ 
f_alu(){ 
set_width(2); 
set_type(opcode); 
set_offset(2); 
}
};
