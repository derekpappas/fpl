csl_enum e_op{ 
  ADD, 
    XOR, 
    SUB
}; 
csl_isa_field f_op{
  f_op(){
    set_width(2);
    set_enum(e_op);
    set_type(opcode);
  }
};
csl_isa_field f_op2(f_op); 


csl_unit top {
};
