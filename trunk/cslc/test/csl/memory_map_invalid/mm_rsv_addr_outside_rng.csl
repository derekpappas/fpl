// this test sets the reserved addr range outside the specified  addr range


csl_memory_map_page mpage{
  mpage(){
    add_address_range(128,255);
    add_reserved_address_range(0,127);
    set_address_increment(2);
  }
};
csl_memory_map mmap{
  mpage mpage;
mmap(){
set_data_word_width(32);
}
};


csl_unit top {
};
