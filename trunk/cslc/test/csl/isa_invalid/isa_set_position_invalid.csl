//illegal: set_position parameters

csl_isa_field op(2);
csl_isa_field field(2);

csl_isa_element format{
    op op_i;
    field field_i;
    format(){
	set_type(instr_format);
       set_width(4);            
       set_position(op_i, 2,3);   //only start position
       set_next(op_i,field_i);
    }
};

