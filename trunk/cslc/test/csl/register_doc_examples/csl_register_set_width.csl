//Monica
//26 oct 2007

csl_register rr{
  rr(){
    set_type(counter);
    set_width(128);
    add_logic(count_direction,down);
  }
};

csl_unit u1{
  csl_port clk(input);
  csl_port p1(input);
  csl_port p2(input);
  csl_port p3(output,128);
  //csl_port p4(input,128);
  rr r2(.reset_(p1),.enable(p2),.clock(clk),.reg_out(p3));
  u1(){
  }
};
