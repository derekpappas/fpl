//Petronela
csl_enum e {
  RD,
  WR,
  EN
};
csl_field f{  
  f(){
  set_enum(e);
  set_width(3);
  }
};
csl_field ff{
  ff(){
    //error we don't have set_width,set_range or set_bitrange
    set_dim_range(7,9,8);//error this is not a method for field ,is for bitrange
  }
};

