//Petronela
csl_enum e {
  RD,
  WR,
  EN
};
csl_field f{  
  f(){
  set_enum(e);
  set_width(3);
  }
};
csl_field ff{
  ff(){
    set_width(20);
    set_number_of_dimensions(6);//error this is not a method for field ,is for bitrange
  }
};

