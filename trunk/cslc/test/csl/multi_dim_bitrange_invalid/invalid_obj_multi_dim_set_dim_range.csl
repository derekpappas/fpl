//author: Ines
//testing: obj_multi_dim.set_dim_range(num_expr, num_expr, num_expr);
//         - param 1 : dimension index(dimension indexes start from 0 and go up to num_of_dims-1)
//         - param 2 : lower_index of the dimension
//         - param 3 : upper_index of the dimension 

csl_bitrange bitrange(10);

csl_multi_dim_bitrange obj_name(10);
obj_name.set_dim_range();
obj_name.set_dim_range(1);
obj_name.set_dim_range(2, 3);
obj_name.set_dim_range(3, 4,);
obj_name.set_dim_range(bla);
obj_name.set_dim_range( , );
obj_name.set_dim_range(, 7, 70);
obj_name.set_dim_range(, 80);
obj_name.set_dim_range(bitrange);
obj_name.set_dim_range(1.7);
