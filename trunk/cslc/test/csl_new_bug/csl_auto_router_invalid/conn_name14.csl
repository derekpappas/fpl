//oanab
//10 july 2008


csl_unit a {
  csl_port x(input);
  a () {}
};

csl_unit b {
  a a0;
  csl_port ar_y_x_in(input,2);
  b () {
 
  }
};

csl_unit c {
  b b0;
  csl_port y(input);
  c () {
    y.connect_by_name(b0.a0.x);
  }
};

