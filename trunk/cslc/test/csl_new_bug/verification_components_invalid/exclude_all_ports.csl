//Monica
//13 november 2007
//invalid test --- exclusion_list for  all ports from dut

csl_unit dut{
    csl_port stim_d(input),stim_v(input);
    csl_port exp_v(output),exp_d(output);
    csl_port clk(input);
  dut(){
    clk.set_attr(clock);
  }
};
csl_vector stim{
  stim(){
    set_unit_name(dut);
    set_direction(input);
    exclusion_list(stim_d,stim_v);
  }
};
csl_vector exp{
  exp(){
    set_unit_name(dut);
    set_direction(output);
    exclusion_list(exp_d, exp_v);
  }
};
csl_testbench tb{
  csl_signal clk(reg);
  dut dut;
  tb(){
    clk.set_attr(clock);
    add_logic(clock, clk, 10, ns);
  }
};
