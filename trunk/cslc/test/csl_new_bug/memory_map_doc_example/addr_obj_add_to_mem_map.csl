//Monica

csl_unit top{
  top(){
  }
};

csl_memory_map_page mpg{
  mpg(){
    add_address_range(0,128);
    set_data_word_width(32);
    set_unit_name(top);
  }
};

csl_memory_map mmap{  
  mmap(){
    set_type(hierarchical);
  }
};
