//

csl_unit u{
};

csl_memory_map_page mpage{
mpage(){
 add_address_range(0,1023);
 set_unit_name(u);
// set_address_increment(1);
}
};

csl_memory_map mmap{
 mmap(){
 set_data_word_width(32);
 set_type(hierarchical);
// set_memory_map_prefix("mm"); // not working in parser
}
};


