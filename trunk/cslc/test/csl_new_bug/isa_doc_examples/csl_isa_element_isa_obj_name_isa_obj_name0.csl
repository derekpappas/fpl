//Monica
//csl_isa_element isa_obj_name [: isa_obj_name0]; 

csl_isa_field f_op{
  f_op(){
set_type(opcode);
set_width(2);
  }};
 csl_isa_element isa { 
  isa() { 
    set_type(root_format); 
    set_width(32); 
  }
};
csl_isa_element format : isa { 
  f_op f_op; 
  format() { 
    set_type(instr_format); 
    set_position(f_op, 15); 
  }
};
csl_isa_element f1: format{
  f1(){
    set_type(instr);
  }};

csl_unit top {
};
