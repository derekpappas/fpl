
csl_interface ifc {
  csl_port x(input);
  csl_port y(input);
  ifc () {}
};

csl_unit a {
  ifc ifc0;
  a () {}
};

csl_unit b {
  ifc ifc1;
  a a0(.ifc0.y(ifc1.y));
  csl_port m(input);
  b () {
    a0.ifc0.x.connect_by_name(m);
  }
};


