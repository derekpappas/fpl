//

csl_unit top {
};

csl_memory_map_page mpag{
  mpag(){
    add_address_range(0,128);
    set_unit_name(top);
  }
};

csl_memory_map mmap{
mmap(){
 set_data_word_width(32);
 set_type(hierarchical);
 }
};
