//Monica
//19 oct 2007

csl_unit a {
  a () {
  }
};

csl_memory_map_page mpage{
  mpage(){
   add_address_range(0,128);
   set_unit_name(a);
   set_access_rights(100, SWR,access_read);
   set_access_rights(200:300, SWR,access_write); // is this how we set the range?
  }
};


csl_memory_map mmap{
        mmap(){
        set_data_word_width(32);
		set_type(hierarchical);
  }
};
