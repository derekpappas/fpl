//daniet
//november 07,2007

csl_unit u{
  u(){}
};

csl_memory_map_page mpage_0{
  mpage_0(){
    add_address_range(0,63);
    set_unit_name(u);
   // set_address_increment(2); // not working in parser
    set_data_word_width(32);
    set_alignment(16);
   }
};

csl_memory_map mmap{
  mmap(){
    set_data_word_width(16);
    set_type(hierarchical);
  }
};
