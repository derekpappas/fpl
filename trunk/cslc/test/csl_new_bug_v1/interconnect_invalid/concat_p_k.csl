//costelc
//port - constant concatenation

csl_unit a{
	csl_port a_in(input);
	csl_port a_out(output,8);
};

csl_unit c{
  csl_port c_in(input,16);
  csl_port c_out(output);
  c() {}
};

csl_unit top {
  a a0; 
 c  c0(.c_in({a0.a_out,8'b11100111}));
  top() {}
};

	
