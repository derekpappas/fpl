csl_enum ym {
	il = 78,
	pp = 26,
	pt = 60,
	uh = 71,
	uu = 80,
	id = 49,
	hi = 64
};
csl_enum re {
	ii,
	ta,
	el,
	yi,
	gv,
	yn
};
csl_enum xx {
	ch = 98,
	lr = 62,
	wc = 69
};
csl_isa_instruction_format pa{
    pa( ){
     generate_decoder( dogefeaa);
  }
}
;
csl_isa_instruction oo : pa{
    oo( ){
  }
}
;
csl_isa_instruction uf : pa{
    uf( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa_instruction ik : pa{
    ik( ){
    set_asm_mnemonic( csl_list "qp", "sg", "gy");
  }
}
;
csl_isa bj{
  ik ie;
  oo kh;
  uf ao;
  ik cu;
  oo ew;
  oo xo;
  ik fw;
  uf dx;
  ik lc;
  uf jn;
  ik by;
  oo xi;
  uf ah;
  oo pq;
  ik oq;
  uf tr;
  oo um;
  uf gc;
  oo dk;
  uf ne;
  oo bk;
  oo ey;
  uf cv;
  ik tt;
  ik kx;
  uf jr;
  ik rf;
  oo ot;
  oo en;
  uf uo;
  ik sj;
  uf ov;
  uf kr;
  oo wa;
  ik cp;
  uf jv;
  ik jd;
    bj( ){
     set_decoder_name( "pw");
     set_decoder_out_name_prefix( "gb");
     set_decoder_out_name_suffix( "nq");
     generate_decoder( dogeaa);
  }
}
;
csl_fifo ks{
   ks( ){
     add_logic( sync_fifo);
     add_logic( wr_hold, 0);
     add_logic( parallel_input, all);
     add_logic( pushback);
     add_logic( string_through, 1);
     add_logic( output_rd_addr);
  }
}
;
csl_memory_map_page px{
    px( ){
     add_address_range( 4, 7);
     add_reserved_address_range( 4, 4);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 8);
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page hr{
    hr( ){
     add_address_range( 2, 0);
     set_address_increment( 7);
     get_address_increment( );
     get_next_address( );
     set_access_rights( access_read_write, access_write);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 6);
     get_data_word_wodth( );
     set_aligment( 4);
     get_aligment( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page nu{
  px jm;
  hr yo;
  px gf;
  hr td;
    nu( ){
     set_address_increment( 0);
     set_next_address( 1);
     get_next_address( );
     set_access_rights( access_read_write, access_read_write);
     add_reserved_address_range( 6, 5);
     get_data_word_wodth( );
     set_aligment( 0);
     get_aligment( );
     set_endianess( big_endian);
     get_endianess( );
  }
}
;
csl_memory_map xq{
    xq( ){
     set_prefix( ej);
     get_prefix( );
  }
}
;
csl_register hc{
    hc( ){
     set_count_direction( 3);
     set_end_value( 1);
     add_logic( inc_signal);
  }
}
;
csl_register ba{
    ba( ){
     add_logic( read_valid);
     set_const_value( 9);
     add_logic( bypass);
  }
}
;
