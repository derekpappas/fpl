csl_enum my {
	vm = 13,
	dn = 40,
	pt = 8,
	qf = 38,
	qt = 43,
	rw = 69,
	lp = 20,
	mh = 90,
	ku = 27
};
csl_enum xc {
	tc,
	vy,
	ew,
	my,
	ky,
	xi,
	lb,
	xe,
	wp,
	ln
};
csl_enum jd {
	mw,
	ua,
	fh,
	wf,
	jr,
	hf,
	sl
};
csl_isa_instruction_format hl{
    hl( ){
     set_width( 7);
     generate_decoder( vdne);
  }
}
;
csl_isa ex{
    ex( ){
     set_decoder_name( "os");
     set_decoder_out_name_prefix( "vf");
     set_decoder_out_name_suffix( "vt");
     generate_decoder( fkvdif);
     print( isa.txt);
  }
}
;
csl_fifo bn{
   bn( ){
     add_logic( programable_depth, 8);
     add_logic( sync_fifo);
     add_logic( depth_extend, 8);
     add_logic( rd_credit);
  }
}
;
csl_memory_map_page vr{
    vr( ){
     add_address_range( 6, 7);
     set_address_increment( 6);
     get_address_increment( );
     add_reserved_address_range( 7, 2);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 4);
     get_data_word_wodth( );
     set_aligment( 5);
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page ap{
  vr fq;
  vr hc;
    ap( ){
     add_address_range( 6, 0);
     set_address_increment( 8);
     set_next_address( 6);
     set_access_rights( access_read, access_read_write);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 4);
     get_data_word_wodth( );
     set_aligment( 5);
     get_aligment( );
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page ns{
  vr yr;
  ap ai;
  ap po;
    ns( ){
     set_address_increment( 0);
     set_next_address( 6);
     set_access_rights( access_read, access_read);
     add_reserved_address_range( 9, 9);
     get_lower_bound( );
     set_data_word_width( 0);
     get_data_word_wodth( );
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map xh{
    xh( ){
     set_top_unit( vdifne);
     set_prefix( et);
     set_suffix( nc);
     get_suffix( );
  }
}
;
csl_register ux{
    ux( ){
     set_width( 4);
     add_logic( wr_en);
     add_logic( gray_output);
     set_start_value( 3);
     add_logic( cnt_dir_signal);
     add_logic( dec_signal);
  }
}
;
csl_register tr{
    tr( ){
     set_width( 1);
     get_width( );
     set_depth( 8);
     add_logic( read_valid);
     add_logic( bypass);
  }
}
;
