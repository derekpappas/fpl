csl_enum us {
	nw,
	ud,
	xv,
	cq,
	tv,
	ry,
	xc,
	hu
};
csl_enum fj {
	ky,
	kd,
	gu,
	xu,
	og,
	yj
};
csl_enum cm {
	iw,
	rh,
	tu,
	ap,
	qr,
	oo,
	wy,
	kc,
	iw
};
csl_isa_instruction_format wj{
    wj( ){
     set_width( 6);
     generate_decoder( kbbw);
  }
}
;
csl_isa_instruction ys : wj{
    ys( ){
    set_asm_mnemonic( fw);
  }
}
;
csl_isa_instruction ky : wj{
    ky( ){
    set_asm_mnemonic( csl_list "cn", "si", "ji", "re");
  }
}
;
csl_isa mh{
    mh( ){
     set_decoder_name( "tg");
     set_decoder_out_name_prefix( "ch");
     set_decoder_out_name_suffix( "rq");
     print( isa.txt);
  }
}
;
csl_fifo fb{
   fb( ){
     add_logic( sync_fifo);
  }
}
;
csl_memory_map_page xx{
    xx( ){
     add_address_range( 5, 2);
     get_address_increment( );
     set_next_address( 2);
     get_next_address( );
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     set_aligment( 3);
     get_aligment( );
     set_endianess( little_endian);
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page xq{
  xx be;
    xq( ){
     set_address_increment( 6);
     set_next_address( 4);
     get_next_address( );
     add_reserved_address_range( 8, 4);
     add( xx);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 4);
     set_aligment( 0);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page nb{
    nb( ){
     add_address_range( 3, 8);
     set_address_increment( 1);
     get_address_increment( );
     set_next_address( 9);
     get_next_address( );
     set_access_rights( access_read_write, none);
     add( xxxq);
     get_upper_bound( );
     set_data_word_width( 0);
     set_aligment( 8);
     get_aligment( );
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map yi{
    yi( ){
     auto_gen_memory_map( );
xx.set_access_rights_enum( xx); 
     set_prefix( td);
     set_suffix( on);
  }
}
;
csl_register ly{
    ly( ){
  }
}
;
csl_register yb{
    yb( ){
     set_width( 8);
     get_width( );
     set_depth( 0);
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 7);
  }
}
;
