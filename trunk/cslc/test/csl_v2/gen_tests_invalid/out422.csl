csl_enum hv {
	ff = 18,
	xt = 72,
	oj = 58,
	ka = 58,
	pc = 70,
	gb = 39,
	lb = 25,
	iv = 2,
	tb = 79
};
csl_enum cj {
	fy = 66,
	mu = 44,
	sm = 85,
	kk = 10,
	rs = 6,
	hy = 37,
	af = 11
};
csl_enum so {
	rd,
	oh,
	hw,
	kt,
	xu,
	pt,
	xu
};
csl_isa_instruction_format bb{
    bb( ){
     set_width( 8);
  }
}
;
csl_isa_instruction ds : bb{
    ds( ){
    set_asm_mnemonic( csl_list "xl", "dn", "ep", "hr", "mk");
  }
}
;
csl_isa gi{
    gi( ){
     set_decoder_name( "cb");
     set_decoder_out_name_suffix( "iy");
     print( isa.txt);
  }
}
;
csl_fifo ft{
   ft( ){
     add_logic( width_extend, 6);
     add_logic( pushback);
  }
}
;
csl_memory_map_page cc{
    cc( ){
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page vr{
    vr( ){
     get_next_address( );
     set_access_rights( none, access_read_write);
     add( cc);
     get_lower_bound( );
     set_data_word_width( 8);
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page wa{
    wa( ){
     add_address_range( 0, 7);
     set_address_increment( 7);
     set_next_address( 7);
     get_next_address( );
     set_access_rights( access_read_write, access_write);
     add_reserved_address_range( 3, 6);
     add( cc);
     get_lower_bound( );
     set_data_word_width( 4);
     get_data_word_wodth( );
     set_aligment( 3);
     get_aligment( );
     set_endianess( little_endian);
     get_symbol_lenght( );
  }
}
;
csl_memory_map nm{
    nm( ){
     auto_gen_memory_map( );
     set_top_unit( pyfrxn);
vr.set_access_rights_enum( ccwa); 
     set_data_word_width( 2);
     get_data_word_width( );
     set_prefix( cf);
     set_suffix( ht);
  }
}
;
csl_register pi{
    pi( ){
     set_range( {[0:6], [0:4]});
     set_type( STATUS);
     set_width( 0);
     add_logic( neg_output);
     add_logic( serial_output);
     set_count_amount( 9);
     set_end_value( 7);
     set_start_value( 72);
     stop_at_end_value( );
     add_logic( inc_signal);
     add_logic( dec_signal);
  }
}
;
csl_register qj{
    qj( ){
     set_depth( 7);
     get_depth( );
     create_rtl_module( );
     set_const_value( 5);
  }
}
;
