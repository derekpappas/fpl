csl_enum cx {
	md = 91,
	ao = 70,
	iw = 16,
	ug = 46,
	qt = 97,
	el = 14,
	le = 23
};
csl_enum mr {
	mh = 10,
	oh = 74,
	co = 48
};
csl_enum wf {
	tp = 84,
	ve = 92,
	ty = 68,
	ei = 17,
	ee = 65,
	wm = 88,
	ds = 95,
	uw = 48,
	oy = 37,
	ic = 55,
	gw = 64,
	tu = 26,
	gi = 46,
	ei = 70,
	yb = 63
};
csl_isa_instruction_format co{
    co( ){
     set_width( 0);
     generate_decoder( qospnx);
  }
}
;
csl_isa oe{
    oe( ){
     set_decoder_name( "nx");
     set_decoder_out_name_prefix( "dr");
     generate_decoder( qo);
  }
}
;
csl_fifo am{
   am( ){
     add_logic( parallel_output, all);
     add_logic( output_rd_addr);
  }
}
;
csl_memory_map_page xm{
    xm( ){
     set_next_address( 4);
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page uh{
  xm jq;
    uh( ){
     get_next_address( );
     add( am, "eq", 4);
     get_aligment( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page bo{
  uh kl;
    bo( ){
     add_address_range( 7, 9);
     set_address_increment( 2);
     get_address_increment( );
     get_next_address( );
     add_reserved_address_range( 4, 8);
     add( xm);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 5);
     get_data_word_wodth( );
     set_aligment( 2);
     get_aligment( );
     set_endianess( big_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map ge{
  xm ej;
  uh ja;
  bo bb;
  xm ag;
  xm rc;
  uh cs;
  bo rx;
    ge( ){
     auto_gen_memory_map( );
     set_top_unit( sf);
     set_type( hierach);
xmuh.set_access_rights_enum( uh); 
     set_data_word_width( 6);
     get_data_word_width( );
     set_prefix( ew);
  }
}
;
csl_register aq{
    aq( ){
     set_width( 4);
     set_lock_enable_bit( 2);
     add_logic( dec_signal);
  }
}
;
csl_register em{
    em( ){
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 5);
     add_logic( bypass);
  }
}
;
