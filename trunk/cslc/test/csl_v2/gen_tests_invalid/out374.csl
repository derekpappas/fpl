csl_enum up {
	ew = 72,
	ud = 16,
	ph = 94,
	qo = 10
};
csl_enum ln {
	mg,
	ir,
	nr,
	si,
	so,
	bt,
	xj
};
csl_isa_instruction_format qd{
    qd( ){
     generate_decoder( scoenr);
  }
}
;
csl_isa ea{
    ea( ){
     set_decoder_name( "pu");
     set_decoder_out_name_prefix( "wt");
     set_decoder_out_name_suffix( "po");
     generate_decoder( nr);
     print( isa.txt);
  }
}
;
csl_fifo qk{
   qk( ){
     set_dimension( 2, 8);
     add_logic( priority_bypass);
     add_logic( rd_words, 0);
     add_logic( pushback);
     add_logic( rd_credit);
  }
}
;
csl_memory_map_page cl{
    cl( ){
     set_access_rights( access_write, none);
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     set_aligment( 5);
     set_endianess( big_endian);
     get_endianess( );
  }
}
;
csl_memory_map_page lf{
    lf( ){
     add_address_range( 2, 1);
     set_address_increment( 3);
     add_reserved_address_range( 4, 9);
     add( qk, "xu", 3);
     add( cl);
     get_upper_bound( );
     get_aligment( );
     set_endianess( little_endian);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page xk{
    xk( ){
     add_address_range( 1, 0);
     get_address_increment( );
     set_next_address( 5);
     get_next_address( );
     add_reserved_address_range( 9, 4);
     add( cllf);
     get_lower_bound( );
     set_aligment( 2);
     get_aligment( );
     get_endianess( );
  }
}
;
csl_memory_map ik{
  cl lb;
  lf nl;
  cl ev;
    ik( ){
     auto_gen_memory_map( );
     set_type( page);
     set_data_word_width( 3);
  }
}
;
csl_register fc{
    fc( ){
     set_range( {[4:6], [5:6], [4:9], [5:4], [7:4]});
     set_type( STATUS);
     set_width( 4);
     index_data_pair( ik, 4);
     constant( 8);
     add_logic( neg_output);
     add_logic( serial_input);
     add_logic( wr_en);
     set_value( 2);
     set_lock_enable_bit( 5);
     get_lock_enable_bit( );
     add_logic( gray_output);
     set_count_direction( 6);
     set_start_value( 2);
     add_logic( inc_signal);
     add_logic( dec_signal);
  }
}
;
csl_register et{
    et( ){
     set_width( 7);
     get_width( );
     set_depth( 1);
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 4);
     add_logic( bypass);
  }
}
;
