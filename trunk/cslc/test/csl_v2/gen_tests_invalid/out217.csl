csl_enum fb {
	uf,
	un
};
csl_enum gd {
	sv = 11,
	vb = 36,
	gt = 46,
	eu = 37,
	wc = 54
};
csl_enum xw {
	ob = 38,
	dd = 81,
	ps = 25
};
csl_isa_instruction_format jn{
    jn( ){
  }
}
;
csl_isa_instruction rc : jn{
    rc( ){
    set_asm_mnemonic( csl_list "ma", "kr");
  }
}
;
csl_isa cb{
    cb( ){
     set_decoder_name( "mi");
     set_decoder_out_name_prefix( "er");
     set_decoder_out_name_suffix( "tp");
     generate_decoder( xh);
     print( isa.txt);
  }
}
;
csl_fifo tr{
   tr( ){
     add_logic( rd_words, 2);
     add_logic( stall);
  }
}
;
csl_memory_map_page sg{
    sg( ){
     add_address_range( 8, 5);
     set_address_increment( 7);
     set_next_address( 8);
     get_next_address( );
     set_access_rights( none, none);
     add_reserved_address_range( 1, 9);
     get_lower_bound( );
     get_aligment( );
     set_endianess( little_endianbig_endian);
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page gr{
  sg ft;
  sg ql;
    gr( ){
     set_address_increment( 2);
     get_next_address( );
     add( tr, "of", 5);
     get_data_word_wodth( );
     set_aligment( 7);
     set_endianess( little_endianbig_endian);
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page nd{
    nd( ){
     add_address_range( 2, 9);
     set_address_increment( 8);
     get_address_increment( );
     set_next_address( 6);
     add_reserved_address_range( 9, 7);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 0);
     get_aligment( );
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map sq{
  sg ha;
  nd mt;
  sg xa;
  nd ko;
  sg ly;
  nd uf;
    sq( ){
     set_type( page);
     set_prefix( rs);
     get_prefix( );
     set_suffix( eu);
  }
}
;
csl_register cv{
    cv( ){
     add_logic( neg_output);
     add_logic( serial_input);
     add_logic( rd_en);
     reset_value( 3);
  }
}
;
csl_register fn{
    fn( ){
     create_rtl_module( );
     set_const_value( 6);
     add_logic( bypass);
  }
}
;
