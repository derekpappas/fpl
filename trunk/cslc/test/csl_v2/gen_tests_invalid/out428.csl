csl_enum jw {
	pb = 11,
	ms = 50,
	td = 88,
	dy = 10,
	sf = 74,
	iw = 94,
	eb = 31,
	hl = 50
};
csl_enum tv {
	uc,
	wy,
	uw,
	gf,
	vm
};
csl_enum kf {
	jc,
	ix,
	xe,
	cb,
	ew
};
csl_isa_instruction_format yl{
    yl( ){
     set_width( 0);
     generate_decoder( qspvep);
  }
}
;
csl_isa aq{
    aq( ){
     set_decoder_name( "oq");
     set_decoder_out_name_prefix( "qk");
     set_decoder_out_name_suffix( "pk");
     generate_decoder( pvep);
     print( isa.txt);
  }
}
;
csl_fifo le{
   le( ){
     add_logic( wr_release);
  }
}
;
csl_memory_map_page lq{
    lq( ){
     add_address_range( 2, 4);
     set_next_address( 7);
     get_next_address( );
     set_data_word_width( 9);
     get_data_word_wodth( );
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page hu{
    hu( ){
     set_address_increment( 6);
     get_next_address( );
     get_lower_bound( );
     get_upper_bound( );
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page ry{
  lq ay;
  lq ny;
  hu ma;
    ry( ){
     set_next_address( 5);
     add_reserved_address_range( 1, 0);
     get_lower_bound( );
     set_data_word_width( 1);
     get_data_word_wodth( );
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map lf{
    lf( ){
     set_top_unit( qsep);
lqhu.set_access_rights_enum( ry); 
     set_data_word_width( 6);
     get_data_word_width( );
     set_prefix( jg);
     set_suffix( ji);
  }
}
;
csl_register xt{
    xt( ){
     set_range( {[6:7], [9:6], [9:9], [4:8], [1:9], [4:7], [3:5]});
     set_type( STATUS);
     reset_value( 5);
     set_lock_enable_bit( 1);
     get_lock_enable_bit( );
  }
}
;
csl_register ys{
    ys( ){
     add_logic( read_valid);
     set_const_value( 8);
  }
}
;
