csl_enum bl {
	ej = 12,
	oi = 1,
	ye = 20,
	ao = 69,
	gt = 28,
	xg = 54,
	gh = 56,
	kn = 55,
	am = 71,
	hc = 16
};
csl_enum nh {
	cr,
	qr,
	lb,
	do,
	oj,
	xf,
	rw,
	js,
	np
};
csl_enum ol {
	xw = 18,
	sl = 47,
	mu = 56,
	xt = 50,
	yo = 56,
	dd = 80
};
csl_isa_instruction_format am{
    am( ){
     set_width( 9);
     generate_decoder( tgaf);
  }
}
;
csl_isa xm{
    xm( ){
     set_decoder_out_name_prefix( "le");
     set_decoder_out_name_suffix( "qq");
     generate_decoder( tgbh);
     print( isa.txt);
  }
}
;
csl_fifo nj{
   nj( ){
     add_logic( programable_depth, 9);
     add_logic( stall_wr_side);
     add_logic( almost_full, 0);
     add_logic( credit);
     add_logic( wr_credit);
  }
}
;
csl_memory_map_page jo{
    jo( ){
     set_address_increment( 7);
     get_address_increment( );
     add_reserved_address_range( 3, 8);
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page nr{
  jo dw;
  jo gn;
  jo bv;
    nr( ){
     add_address_range( 6, 4);
     set_next_address( 0);
     add_reserved_address_range( 2, 4);
     get_aligment( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page gb{
  jo nb;
    gb( ){
     set_address_increment( 2);
     get_address_increment( );
     set_access_rights( access_read, none);
     set_aligment( 3);
     get_aligment( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map ru{
  jo fq;
  gb hi;
  nr pe;
  gb cu;
  jo mr;
  nr oe;
    ru( ){
     auto_gen_memory_map( );
     set_type( hierach);
     set_suffix( xo);
  }
}
;
csl_register al{
    al( ){
     set_type( STATIC);
     set_address( ru, 6);
     index_data_pair( ru, 6);
     constant( 9);
     add_logic( rd_en);
     set_value( 3);
     reset_value( 7);
     add_logic( cnt_dir_signal);
     add_logic( dec_signal);
  }
}
;
csl_register hb{
    hb( ){
     set_width( 8);
     get_width( );
     set_depth( 7);
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 9);
  }
}
;
