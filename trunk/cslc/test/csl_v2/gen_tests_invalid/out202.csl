csl_enum gr {
	ac,
	gw,
	gs,
	en,
	hc,
	eh,
	if,
	rn,
	lc
};
csl_enum oj {
	xb = 37
};
csl_enum qg {
	ox = 61,
	vr = 74,
	cc = 6
};
csl_isa ry{
    ry( ){
     set_decoder_out_name_prefix( "nx");
     set_decoder_out_name_suffix( "do");
     generate_decoder( mq);
     print( isa.txt);
  }
}
;
csl_fifo la{
   la( ){
     add_logic( stall);
  }
}
;
csl_memory_map_page ie{
    ie( ){
     add_address_range( 4, 8);
     set_address_increment( 8);
     get_lower_bound( );
     get_data_word_wodth( );
     set_endianess( little_endianlittle_endian);
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page ks{
    ks( ){
  }
}
;
csl_memory_map yi{
    yi( ){
     set_top_unit( mq);
     set_type( hierach);
ieks.set_access_rights_enum( ks); 
     set_data_word_width( 6);
     get_data_word_width( );
     set_prefix( tc);
     set_suffix( yi);
  }
}
;
csl_register ih{
    ih( ){
     set_type( INT);
     set_atribute( read_write);
     add_logic( neg_output);
     set_count_direction( 4);
     set_end_value( 6);
  }
}
;
csl_register fg{
    fg( ){
     set_width( 9);
     set_depth( 0);
     create_rtl_module( );
     add_logic( bypass);
  }
}
;
