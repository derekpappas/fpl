csl_enum ge {
	ug = 61,
	qs = 32,
	bw = 26
};
csl_enum rk {
	yo = 90,
	tn = 77,
	kk = 79,
	yp = 52,
	qg = 99,
	be = 64,
	dc = 83,
	hs = 50,
	jq = 95,
	uo = 74,
	kb = 64
};
csl_enum sh {
	hj = 28
};
csl_isa_instruction_format ss{
    ss( ){
     generate_decoder( rigjjw);
  }
}
;
csl_isa pe{
    pe( ){
     generate_decoder( jorigjjw);
  }
}
;
csl_memory_map_page cs{
    cs( ){
     add_address_range( 7, 5);
     set_address_increment( 1);
     set_next_address( 3);
     add_reserved_address_range( 8, 4);
     set_data_word_width( 6);
     set_aligment( 9);
     set_endianess( little_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page ry{
    ry( ){
     set_next_address( 6);
     set_access_rights( access_read_write, access_write);
     add_reserved_address_range( 1, 4);
     set_aligment( 7);
     set_endianess( little_endian);
  }
}
;
csl_memory_map_page mk{
  cs vp;
  cs bg;
    mk( ){
     add_address_range( 6, 4);
     set_address_increment( 2);
     set_next_address( 9);
     set_access_rights( access_read, none);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 8);
     set_aligment( 5);
     get_aligment( );
     set_endianess( big_endianbig_endian);
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map nf{
  ry kp;
  cs hg;
  mk nb;
    nf( ){
     set_top_unit( gj);
     set_type( page);
     set_prefix( vp);
  }
}
;
csl_register yg{
    yg( ){
     set_range( [6:7]);
     set_atribute( shadow);
     add_logic( serial_input);
     add_logic( wr_en);
     reset_value( 0);
     set_lock_enable_bit( 8);
     stop_at_end_value( );
     add_logic( cnt_dir_signal);
  }
}
;
csl_register gv{
    gv( ){
     set_width( 7);
     set_depth( 7);
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 7);
     add_logic( bypass);
  }
}
;
