csl_enum mm {
	qs = 82,
	dk = 51,
	yd = 85
};
csl_enum hy {
	ui,
	uc,
	bj,
	pp,
	td,
	nq,
	gj,
	qv
};
csl_enum sw {
	aj,
	bn,
	wt,
	ic,
	ei,
	fl,
	ha
};
csl_isa_instruction_format nu{
    nu( ){
     generate_decoder( qhvr);
  }
}
;
csl_isa_instruction kq : nu{
    kq( ){
  }
}
;
csl_isa_instruction qb : nu{
    qb( ){
    set_asm_mnemonic( csl_list "uw", "hx", "ks", "se", "eh", "je", "pd", "xd");
  }
}
;
csl_isa_instruction dr : nu{
    dr( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa_instruction ad : nu{
    ad( ){
    set_asm_mnemonic( hj);
  }
}
;
csl_isa_instruction af : nu{
    af( ){
    set_asm_mnemonic( tx);
  }
}
;
csl_isa kg{
    kg( ){
     set_decoder_name( "ey");
     set_decoder_out_name_prefix( "ij");
     set_decoder_out_name_suffix( "ob");
     generate_decoder( vr);
     print( isa.txt);
  }
}
;
csl_fifo vm{
   vm( ){
     add_logic( priority_bypass);
     add_logic( rd_credit);
  }
}
;
csl_memory_map_page jp{
    jp( ){
     set_next_address( 8);
     get_next_address( );
     add( vm, "er", );
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 0);
     get_data_word_wodth( );
     set_aligment( 9);
     set_endianess( little_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page sq{
    sq( ){
  }
}
;
csl_memory_map_page pw{
    pw( ){
     set_address_increment( 4);
     get_address_increment( );
     get_next_address( );
     add_reserved_address_range( 8, 6);
     add( jp);
     get_lower_bound( );
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_register mh{
    mh( ){
     set_range( {[6:6], [7:9], [8:1], [6:5], [6:5], [7:6]});
     set_type( INT);
     add_logic( neg_output);
     add_logic( serial_input);
     add_logic( serial_output);
     init_value( 6);
     reset_value( 2);
     set_lock_enable_bit( 3);
     get_lock_enable_bit( );
     add_logic( gray_output);
     set_count_direction( 0);
  }
}
;
csl_register gw{
    gw( ){
     set_width( 2);
     get_width( );
     set_depth( 1);
  }
}
;
