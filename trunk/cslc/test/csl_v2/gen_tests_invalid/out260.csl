csl_enum fl {
	cq,
	bu,
	cl,
	dx,
	vy,
	hy,
	or,
	ed,
	xc,
	vc,
	ja,
	tm
};
csl_enum gj {
	on = 70,
	rw = 67,
	yu = 41,
	nc = 95,
	rr = 11,
	nv = 68
};
csl_enum pl {
	wy = 22,
	km = 42,
	rp = 58,
	rr = 18
};
csl_isa_instruction_format bq{
    bq( ){
     set_width( 8);
     generate_decoder( rtkh);
  }
}
;
csl_isa lo{
    lo( ){
     set_decoder_name( "nt");
     generate_decoder( kh);
  }
}
;
csl_fifo fp{
   fp( ){
     add_logic( stall);
     add_logic( output_wr_addr);
     add_logic( output_rd_addr);
     add_logic( flow);
  }
}
;
csl_memory_map_page dn{
    dn( ){
     set_address_increment( 1);
     set_next_address( 6);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 5);
     get_aligment( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page ty{
    ty( ){
     add_address_range( 4, 9);
     get_address_increment( );
     set_access_rights( none, none);
     add_reserved_address_range( 2, 6);
     add( dn);
     get_lower_bound( );
     set_endianess( big_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page kl{
    kl( ){
     set_address_increment( 6);
     get_address_increment( );
     set_next_address( 9);
     get_next_address( );
     get_lower_bound( );
     set_aligment( 7);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map gq{
    gq( ){
     set_top_unit( rtkh);
.add_to_memory_map( 0,access_write); 
     set_prefix( yh);
     get_prefix( );
     set_suffix( gs);
  }
}
;
csl_register mp{
    mp( ){
     set_type( INT);
     set_atribute( read_write);
     add_logic( serial_input);
     add_logic( serial_output);
     add_logic( wr_en);
     init_value( 1);
     reset_value( 0);
     add_logic( gray_output);
     stop_at_end_value( );
     add_logic( cnt_dir_signal);
  }
}
;
csl_register ji{
    ji( ){
     set_width( 5);
     add_logic( read_valid);
     set_const_value( 9);
     add_logic( bypass);
  }
}
;
