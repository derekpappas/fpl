csl_enum ff {
	ep,
	lh,
	dn,
	pw,
	va,
	xp,
	rv,
	iv,
	bc,
	qs,
	tc,
	op,
	jw,
	dv,
	ej,
	ck
};
csl_enum bn {
	sg,
	dk,
	dy
};
csl_enum me {
	pe,
	aa,
	dd,
	dv,
	xy,
	am,
	qm
};
csl_isa_instruction_format be{
    be( ){
     set_width( 7);
     generate_decoder( lh);
  }
}
;
csl_isa ay{
    ay( ){
     set_decoder_name( "ls");
     set_decoder_out_name_prefix( "ui");
     set_decoder_out_name_suffix( "mt");
     generate_decoder( lhsnkv);
     print( isa.txt);
  }
}
;
csl_fifo qe{
   qe( ){
     add_logic( depth_extend, 9);
     add_logic( rd_words, 7);
     add_logic( wr_words, 4);
     add_logic( almost_full, 5);
  }
}
;
csl_memory_map_page du{
    du( ){
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 8);
     set_endianess( big_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page qy{
    qy( ){
     add_address_range( 4, 9);
     get_address_increment( );
     set_next_address( 6);
     set_access_rights( none, none);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 5);
     get_data_word_wodth( );
     set_aligment( 0);
     get_aligment( );
     set_endianess( big_endianbig_endian);
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page qs{
  qy ds;
  du xp;
    qs( ){
     add_address_range( 6, 5);
     get_address_increment( );
     get_next_address( );
     add_reserved_address_range( 3, 7);
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     get_aligment( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map iv{
    iv( ){
     set_type( hierach);
duqy.set_access_rights_enum( du); 
     set_prefix( ot);
     get_prefix( );
     set_suffix( ki);
  }
}
;
csl_register ct{
    ct( ){
     set_atribute( read_write);
     constant( 7);
     init_value( 8);
     clear_value( 1);
     set_end_value( 0);
     stop_at_end_value( );
     add_logic( cnt_dir_signal);
     add_logic( inc_signal);
  }
}
;
csl_register cv{
    cv( ){
     set_width( 8);
     get_width( );
     set_depth( 6);
     create_rtl_module( );
     set_const_value( 9);
  }
}
;
