csl_enum tr {
	gl,
	od,
	jm,
	mn,
	gf,
	kn
};
csl_enum vk {
	ql = 98,
	fg = 7,
	rt = 27,
	lk = 59
};
csl_enum bi {
	ki = 26,
	iq = 69,
	mc = 60,
	ks = 54,
	ny = 67,
	ww = 21
};
csl_isa_instruction_format rh{
    rh( ){
  }
}
;
csl_isa_instruction nx : rh{
    nx( ){
    set_asm_mnemonic( csl_list "gv");
  }
}
;
csl_isa_instruction jj : rh{
    jj( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa bw{
    bw( ){
     set_decoder_name( "dp");
     generate_decoder( pj);
  }
}
;
csl_fifo bg{
   bg( ){
     add_logic( programable_depth, 5);
     add_logic( parallel_output, all);
     add_logic( stall);
     add_logic( stall_rd_side);
     add_logic( stall_wr_side);
  }
}
;
csl_memory_map_page ml{
    ml( ){
     add_address_range( 9, 2);
     add_reserved_address_range( 9, 1);
     get_data_word_wodth( );
     set_aligment( 0);
     set_endianess( big_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page cs{
    cs( ){
     add_address_range( 6, 4);
     set_address_increment( 8);
     set_next_address( 3);
     add_reserved_address_range( 2, 9);
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     get_aligment( );
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page tf{
    tf( ){
     get_address_increment( );
     set_next_address( 7);
     set_access_rights( access_write, access_read);
     add_reserved_address_range( 2, 5);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 5);
     get_data_word_wodth( );
     set_aligment( 2);
     get_aligment( );
     get_endianess( );
  }
}
;
csl_memory_map bo{
  ml or;
  ml cf;
  cs tm;
  ml tw;
  cs xj;
  tf ts;
    bo( ){
     auto_gen_memory_map( );
     set_top_unit( ql);
     set_type( page);
     set_data_word_width( 0);
     get_data_word_width( );
     set_prefix( pn);
     set_suffix( rs);
  }
}
;
csl_register wf{
    wf( ){
     stop_at_end_value( );
  }
}
;
csl_register fu{
    fu( ){
     add_logic( read_valid);
  }
}
;
