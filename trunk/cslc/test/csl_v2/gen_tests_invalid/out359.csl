csl_enum sk {
	ye,
	ug,
	fv,
	sj,
	aj
};
csl_enum tb {
	gr = 23,
	qj = 21,
	co = 99
};
csl_enum hc {
	ls = 3,
	jv = 16,
	pc = 18,
	iy = 20,
	hd = 65,
	mi = 24,
	hj = 87,
	qm = 78,
	fj = 11,
	yf = 57,
	uu = 20
};
csl_isa_instruction_format rs{
    rs( ){
     set_width( 7);
     generate_decoder( auklhf);
  }
}
;
csl_isa_instruction pe : rs{
    pe( ){
    set_asm_mnemonic( csl_list "ps", "tf");
  }
}
;
csl_isa du{
  pe et;
  pe ao;
  pe wf;
  pe rt;
  pe ah;
  pe jr;
  pe jb;
  pe rc;
  pe ro;
  pe bg;
  pe sh;
  pe xn;
  pe cr;
  pe dj;
    du( ){
     set_decoder_name( "pf");
     set_decoder_out_name_prefix( "if");
     generate_decoder( auklhfwb);
     print( isa.txt);
  }
}
;
csl_fifo st{
   st( ){
     add_logic( programable_depth, 8);
     add_logic( async_fifo);
     add_logic( rd_words, 7);
     add_logic( output_rd_addr);
     add_logic( rd_credit);
     add_logic( flow);
  }
}
;
csl_memory_map_page ix{
    ix( ){
     add_address_range( 3, 3);
     set_address_increment( 8);
     get_next_address( );
     add_reserved_address_range( 2, 2);
     add( st, "hc");
     set_data_word_width( 1);
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page id{
    id( ){
     add_address_range( 2, 0);
     set_next_address( 1);
     get_next_address( );
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     set_aligment( 2);
     get_aligment( );
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page cq{
  ix bm;
  id jt;
  ix mn;
    cq( ){
     get_address_increment( );
     set_next_address( 3);
     get_next_address( );
     set_access_rights( access_read, access_read);
     add_reserved_address_range( 1, 2);
     set_data_word_width( 4);
     get_data_word_wodth( );
     set_endianess( big_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map ys{
    ys( ){
     auto_gen_memory_map( );
.     set_type( page);
     set_data_word_width( 5);
     set_prefix( hr);
     set_suffix( yv);
  }
}
;
csl_register np{
    np( ){
     set_atribute( shadow);
     add_logic( neg_output);
     add_logic( wr_en);
     init_value( 9);
     set_count_direction( 9);
     set_end_value( 1);
     set_start_value( 3);
     add_logic( dec_signal);
  }
}
;
csl_register dq{
    dq( ){
     set_depth( 4);
     get_depth( );
     add_logic( read_valid);
  }
}
;
