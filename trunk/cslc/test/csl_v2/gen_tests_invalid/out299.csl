csl_enum yu {
	xy = 85,
	kd = 84,
	sh = 2,
	yd = 74,
	fi = 67,
	bl = 32,
	nw = 56,
	vj = 11,
	eo = 18,
	un = 53
};
csl_enum gp {
	bg = 73,
	rm = 57,
	ht = 2,
	ci = 63,
	qc = 0,
	ib = 65
};
csl_enum id {
	jj,
	gq,
	rh,
	eg,
	eb,
	rk,
	po
};
csl_isa hj{
    hj( ){
     set_decoder_name( "fv");
     set_decoder_out_name_prefix( "nu");
     set_decoder_out_name_suffix( "kd");
     generate_decoder( rgvlhb);
     print( isa.txt);
  }
}
;
csl_fifo mq{
   mq( ){
     add_logic( async_fifo);
     add_logic( width_extend, 8);
     add_logic( wr_hold, 1);
     add_logic( credit);
  }
}
;
csl_memory_map_page st{
    st( ){
     get_data_word_wodth( );
  }
}
;
csl_memory_map_page bs{
    bs( ){
     set_address_increment( 9);
     get_address_increment( );
     get_next_address( );
     get_lower_bound( );
     set_data_word_width( 3);
     get_data_word_wodth( );
     set_aligment( 3);
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page dm{
    dm( ){
     add_address_range( 8, 9);
     set_address_increment( 0);
     get_address_increment( );
     get_next_address( );
     add_reserved_address_range( 4, 4);
     add( stbs);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 7);
     get_data_word_wodth( );
     set_aligment( 6);
     get_symbol_lenght( );
  }
}
;
csl_memory_map hu{
  st fo;
  st kr;
  dm mx;
  bs xa;
    hu( ){
     auto_gen_memory_map( );
     set_top_unit( ytvlhb);
     set_type( hierach);
stdm.set_access_rights_enum( dm); 
     set_data_word_width( 4);
     get_data_word_width( );
     set_prefix( dy);
  }
}
;
csl_register nm{
    nm( ){
     set_range( {[6:8], [2:0], [3:6], [1:1]});
     constant( 2);
     add_logic( serial_output);
     set_value( 3);
     set_lock_enable_bit( 6);
     add_logic( gray_output);
     set_start_value( 96);
     stop_at_end_value( );
     add_logic( cnt_dir_signal);
     add_logic( inc_signal);
  }
}
;
csl_register hd{
    hd( ){
     set_width( 8);
     get_width( );
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 0);
  }
}
;
