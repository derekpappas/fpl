csl_enum al {
	ad = 22,
	mh = 74,
	lu = 37,
	lf = 11,
	rm = 53,
	ab = 55,
	nj = 59
};
csl_enum jp {
	af = 79,
	jc = 83,
	ty = 34,
	gu = 55,
	je = 98,
	ku = 47,
	hk = 11,
	ij = 66,
	nu = 55,
	rc = 97,
	sf = 31,
	jk = 1,
	rl = 24,
	nh = 30,
	uw = 30,
	wx = 87,
	ci = 97,
	ax = 68,
	jh = 31,
	uv = 97
};
csl_enum li {
	qu = 88,
	ua = 74,
	el = 93,
	wx = 44,
	pr = 29,
	dy = 60,
	cx = 3,
	nr = 56,
	fk = 34,
	of = 94,
	is = 98,
	nj = 35
};
csl_isa_instruction_format xq{
    xq( ){
     set_width( 1);
  }
}
;
csl_isa lh{
    lh( ){
     set_decoder_out_name_prefix( "bu");
     generate_decoder( kmtn);
     print( isa.txt);
  }
}
;
csl_fifo bn{
   bn( ){
     add_logic( stall);
     add_logic( flow);
  }
}
;
csl_memory_map_page np{
    np( ){
     add_address_range( 1, 0);
     get_address_increment( );
     get_next_address( );
     set_data_word_width( 5);
     get_data_word_wodth( );
     get_endianess( );
  }
}
;
csl_memory_map_page dd{
  np oh;
    dd( ){
     get_address_increment( );
     get_next_address( );
     add_reserved_address_range( 0, 2);
     get_aligment( );
  }
}
;
csl_memory_map_page wf{
    wf( ){
     add_address_range( 0, 0);
     set_address_increment( 6);
     set_next_address( 0);
     add_reserved_address_range( 2, 8);
     get_lower_bound( );
     get_upper_bound( );
     set_aligment( 1);
     set_endianess( big_endian);
  }
}
;
csl_memory_map ng{
  dd ym;
  np hn;
  dd ks;
  wf cj;
    ng( ){
     set_top_unit( kmtn);
npdd.add_to_memory_map( 5,access_read); 
     set_type( page);
     set_data_word_width( 5);
     set_prefix( up);
     get_prefix( );
  }
}
;
csl_register xf{
    xf( ){
     constant( 2);
     set_start_value( 34);
     stop_at_end_value( );
  }
}
;
csl_register gc{
    gc( ){
     set_width( 9);
     set_depth( 8);
  }
}
;
