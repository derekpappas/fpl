csl_enum ne {
	fy = 94,
	at = 14,
	vq = 65,
	gv = 69,
	qo = 96,
	df = 87
};
csl_enum bc {
	pg,
	sw,
	fj,
	xh,
	rc,
	aj,
	vo,
	cm,
	uh,
	lu,
	bj,
	ok
};
csl_isa_instruction_format kx{
    kx( ){
     set_width( 2);
  }
}
;
csl_isa ys{
    ys( ){
     set_decoder_name( "ar");
     set_decoder_out_name_suffix( "xb");
  }
}
;
csl_fifo xv{
   xv( ){
     add_logic( width_extend, 5);
     add_logic( wr_hold, 5);
  }
}
;
csl_memory_map_page jn{
    jn( ){
     add_address_range( 4, 3);
     get_address_increment( );
     set_next_address( 1);
     set_access_rights( none, access_write);
     set_data_word_width( 4);
     get_data_word_wodth( );
     get_aligment( );
     set_endianess( little_endianlittle_endian);
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page rm{
  jn fp;
  jn sj;
  jn cl;
    rm( ){
     add_address_range( 7, 4);
     set_address_increment( 2);
     get_address_increment( );
     set_next_address( 4);
     add_reserved_address_range( 2, 3);
     add( jn);
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     set_aligment( 6);
     get_aligment( );
     set_endianess( big_endian);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page hh{
    hh( ){
     add_address_range( 5, 0);
     set_address_increment( 8);
     get_next_address( );
     add_reserved_address_range( 8, 8);
     get_lower_bound( );
     set_data_word_width( 3);
     get_data_word_wodth( );
     set_aligment( 6);
     get_aligment( );
     set_endianess( little_endian);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map ax{
    ax( ){
     auto_gen_memory_map( );
     set_top_unit( blvovb);
     set_data_word_width( 6);
     set_prefix( aj);
  }
}
;
csl_register ve{
    ve( ){
     set_range( {[8:3], [1:3], [0:2], [7:4]});
     add_logic( neg_output);
     add_logic( rd_en);
     reset_value( 5);
     set_count_direction( 4);
  }
}
;
csl_register xh{
    xh( ){
     set_width( 3);
     get_width( );
     set_depth( 9);
     add_logic( read_valid);
     create_rtl_module( );
  }
}
;
