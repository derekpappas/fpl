csl_enum hq {
	cb = 89,
	jq = 41,
	ct = 79,
	xh = 93,
	gt = 53,
	jq = 7,
	ql = 18,
	vf = 29,
	lb = 93
};
csl_enum yg {
	me,
	qr,
	ww,
	bk,
	bi,
	df
};
csl_isa_instruction_format mb{
    mb( ){
     set_width( 9);
  }
}
;
csl_isa_instruction nl : mb{
    nl( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa ew{
    ew( ){
     set_decoder_name( "co");
     set_decoder_out_name_suffix( "ve");
     generate_decoder( pbvfai);
     print( isa.txt);
  }
}
;
csl_fifo hi{
   hi( ){
  }
}
;
csl_memory_map_page gu{
    gu( ){
     add_address_range( 6, 4);
     set_access_rights( none, none);
     add_reserved_address_range( 5, 6);
     get_lower_bound( );
     set_data_word_width( 5);
     get_data_word_wodth( );
     set_aligment( 0);
     get_aligment( );
     set_endianess( big_endian);
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page ef{
    ef( ){
     set_address_increment( 2);
     get_address_increment( );
     add_reserved_address_range( 8, 4);
     add( gu);
     get_lower_bound( );
     set_data_word_width( 2);
     get_data_word_wodth( );
     set_aligment( 7);
     set_endianess( big_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page kp{
    kp( ){
     set_address_increment( 5);
     set_data_word_width( 3);
     set_endianess( big_endianlittle_endian);
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map bs{
    bs( ){
     auto_gen_memory_map( );
     set_top_unit( pb);
     set_data_word_width( 3);
     get_data_word_width( );
     set_prefix( we);
     get_prefix( );
     set_suffix( ds);
  }
}
;
csl_register sd{
    sd( ){
     set_range( [8:6]);
     set_width( 9);
     get_atributes( );
     add_logic( wr_en);
     init_value( 7);
     set_value( 4);
     reset_value( 3);
     clear_value( 0);
     set_lock_enable_bit( 3);
     add_logic( gray_output);
     set_count_amount( 4);
     set_start_value( 43);
     stop_at_end_value( );
     add_logic( cnt_dir_signal);
     add_logic( inc_signal);
  }
}
;
csl_register vd{
    vd( ){
     set_width( 5);
     get_width( );
     set_depth( 7);
     set_const_value( 6);
     add_logic( bypass);
  }
}
;
