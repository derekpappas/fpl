//bug 976
csl_unit top{
  csl_signal d_data1,d_data2,data_in,data_out,st;
    top(){
    st.set_attr(en);
    data_out.set_attr(st.get_attr());
  }
};

csl_interface p{
csl_port x(output,1),y(output,1),z(output,1),a(input,1), b(input,1),
c(input,1);
p(){}
};
csl_unit P{
p p0;
P(){}
};
csl_unit Q{
p q0;
Q(){
q0.reverse(); }
};

/*
csl_unit reg1{
  csl_port  out(output);
  reg1(){
out.set_range(31, 0);}
};
*/

