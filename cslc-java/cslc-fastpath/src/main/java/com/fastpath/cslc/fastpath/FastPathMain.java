package com.fastpath.cslc.fastpath;

/**
 * Placeholder for {@code trunk/cslc/trunk/src/FastPath/main.cpp} (Qt {@code QApplication} + {@code MainWindow}). A
 * JavaFX entry point will replace this once the GUI port lands in {@code cslc-gui-javafx}.
 */
public final class FastPathMain {

    public static void main(String[] args) {
        System.err.println(
                "[fastpath] GUI not ported: legacy Qt main is a stub until JavaFX wiring exists (args=" + args.length + ").");
    }

    private FastPathMain() {}
}
