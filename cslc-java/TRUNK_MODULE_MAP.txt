Mapping: trunk/cslc/trunk → cslc-java Maven modules
====================================================
Machine-readable list of EVERY file: run
  python3 cslc-java/scripts/inventory_trunk.py
which writes TRUNK_FILE_INVENTORY.tsv and TRUNK_FILE_INVENTORY_SUMMARY.json.


trunk/cslc/trunk (top level)
  3rd_party_libs/stringTemplate  → cslc-stringtemplate (Java ST4 dependency)
  antlr/                         → (excluded — legacy ANTLR 2 toolchain/runtime)
  assert/                        → cslc-support (assertions)
  build/                         → (build output — no module)
  build_utils/                   → cslc-build-utils
  csl_gen/                       → cslc-csl-gen
  doc/                           → (docs — no module yet)
  driver/                        → cslc-driver
  include/                       → cslc-support / shared headers → Java types
  libc/                          → cslc-libc-stub (native placeholder)
  old_src/                       → (archived — no module)
  scripts/                       → cslc-java/regression/python (harness)
  systemc/                       → cslc-systemc-assets
  templates/                     → cslc-templates
  src/aa/                        → cslc-aa
  src/autorouter/                → cslc-autorouter
  src/cdom/                      → cslc-cdom
  src/csim/                      → cslc-csim
  src/csl_command_shell/         → cslc-command-shell
  src/csl_xml_warn_error/        → cslc-xml-warn-error
  src/cslc/                      → cslc-core (orchestration; was split from CLI)
  src/cslom/                     → cslc-cslom
  src/cslom_design_checker/      → cslc-design-checker
  src/cslom_generators/          → cslc-generators
  src/cslom2cdom_adapter/        → cslc-adapter
  src/FastPath/                  → cslc-fastpath
  src/gui_cslom/                 → cslc-gui-cslom
  src/ide/                       → cslc-ide
  src/license/                   → cslc-license
  src/parser/csl + grammars      → cslc-grammars + cslc-parser-csl
  src/parser/verilog             → cslc-parser-verilog
  src/preproc/                   → cslc-preproc
  src/support/                   → cslc-support
  trunk/include/ (boost fs)      → cslc-support LegacyBoostFilesystemNote
  trunk/assert/                → cslc-assert
  trunk/build_utils/main.cpp   → cslc-build-utils BuildUtilsCsimMain + cslc-csim shell stub
  trunk/scripts/*.pl           → regression/python/trunk_scripts (see SCRIPT_INDEX.tsv)
  src/t/                         → (sample .csl — no module)
  src/wincslc/                   → cslc-wincslc

trunk/cslc/misc (separate tree, ~4800 files — not all Java)
  See MISC_PORT_MAP.txt and run:  python3 cslc-java/scripts/inventory_misc.py

Incremental port (Java, vs TRUNK_FILE_INVENTORY.tsv backlog)
  cslc-cslom: CSLOM_Declarations.h slice → CslomNodeType + CslomDeclarations; CSLOM_Base.h slice → CslomBase,
    CslomPlaceholder; CSLOM.cpp → CslomUtil (ltos, checkIdName); CSLOM_Visitor.h slice → visitor/CslomVisitor,
    CslomTraversal.preorder
  cslc-generators: NoopCslomTraversal; CslomNodeCounter, CslomPreorderTypeRecorder, CslomIndentedDebugVisitor
    (preorder indent + nodeType/line/col/file trace toward CVisitorTraversal / src/cslom_generators/)
  cslc-csim: CsimSimulatorPort/Stub (getClockByName, getUnitByName), CsimClockPort/CsimClockStub,
    CsimUnitPort (unitName/instanceName) / CsimUnitStub; CsimCmdLinePort/Stub; vector-writer marker (Csim*.h)
  cslc-support: include/src/convenience.cpp → FilesystemConvenience (nio)
  cslc-csl-gen: csl_gen_base.cpp/h slice → CslGenCslBase (getRegisteredNames = m_names view, getChildrenOfType),
    CslGenScope (addToScope/removeFromScope; CslGenCslBase delegates interconnect*Scope),
    CslGenWireTables, CslGenInt; csl_gen_design →
    CslGenDesign; csl_gen_unit → CslGenUnit; csl_code_gen.cpp → CslCodeGenMain (--print/-p, --seed, --ao-batch dir [n],
    createRng); cGen_ao.cpp → CGenConsts (LEGAL_/ILLEGAL_TEST_FILE_NAME_PREFIX), CslGenAoBatch (LEGACY_DEFAULT_FILE_COUNT,
    writeCslFiles)
  cslc-xml-warn-error: CSLC_Errors.h slice → CslcLegacyErrorCodes; we_logic addError slice → CslWeCollector
  cslc-driver: WE_Driver.cpp → WeDriverMain
  cslc-csl-gen: scripts/rand.cpp → CslGenRandString; support.cpp IntToString → CslGenInt.intToString; support.cpp
    emit helpers → CslGenSupportEmit; numExpr / randNumExpr / createTree / displayTree → CslGenNumExpr +
    CslGenNumExprSupport; UNARY_POSTFIXED_OP / BINARY_OP → CslGenExprTables; cslMemory.h/cpp → CslGenMemoryTables,
    CslGenMemory (getMemoryUsedAt, getDepthText/getWidthText), CslGenDesign.addMemory; cslMemoryMap.h/cpp → CslGenMemoryMapTables,
    CslGenMemMapPage (getMemMapPageUsedAt + m_* Text getters + m_*F flags), CslGenMemMap
    (getMemMapUsedAt + m_* Text getters + list snapshots + m_*F flags), CslGenDesign.addMemMapPage / addMemMap; cslIsa.h/cpp → CslGenIsaTables,
    CslGenIsaField (getIsaFieldUsedAt, getIsaFieldTypeDeclUsedAt, m_* Text getters, list snapshots, m_*F flags),
    CslGenIsaElement (getIsaElementUsedAt, m_* Text getters, list snapshots, m_*F flags),
    CslGenDesign.addIsaField / addIsaElement; cslField.h/cpp → CslGenFieldTables,
    CslGenField (getFieldUsedAt, getFieldTypeDeclUsedAt, m_* Text getters, list snapshots, m_*F flags), CslGenFieldInst
    (getFieldInstTypeName/getFieldInstText = m_field_inst, getInstObj/getInstField),
    CslGenDesign.addField;
    cslRegister.h/cpp slice → CslGenRegisterTables, CslGenRegister (getRegisterUsedAt + m_* Text getters + m_*F flags),
    CslGenDesign.addRegister, CslGenRegisterFile (all m_* Text getters, m_*F flags, getRegisterFileUsedAt),
    CslGenDesign.addRegisterFile; CSLinstance → CslGenInstance (getInstantiatedObj/getInstObj = m_instObj);
    CSLunit::addRegFileInst → CslGenUnit.addRegFileInst; cGenFifo_ao.h/cpp → CslGenFifoTables, CslGenFifo
    (m_depth/m_width + other m_* getters, getFifoUsedAt = used[]), CslGenDesign.addFifo, CslGenUnit.addFifoInst;
    cslInterconnectGen_TB slice →
    CslGenInterconnectConsts
    (NAMES_SIZE, tbRandString = CSLbase::randString),
    CslGenBitrangeState, CslGenBitrange (copyBitrange, getDeclForm = m_declForm), CslGenPort
    (PORT_DECL_COPY, getBitrCopyText = m_bitrCopy, getDeclForm), CslGenSignal
    (getBitrCopyText/getSigCopyText, getDeclForm),
    CslGenScopedSelection (first/second = legacy pair),
    CslGenCslBase.randSelObj,
    CslGenCslBase.newNameIsValid (parent-chain + enum items),
    CslGenIfc, CslGenIfcInst (getIfcType/getInstType/getIfc, randSelIfc), CslGenUnitInst, CslGenEnum (getEnumItems),
    CslGenEnumItem (getValue, isValueSet/isValueF = m_valueF),
    CslGenPort/CslGenSignal.getBitrangeState (= getBitrange), CslGenDesign.addEnum +
    CslGenDesign.buildDecl(RandomGenerator), CslGenDesign.addBitrange/addSignal/addIfc/addUnit,
    CslGenUnit.randSelPort/randSelSignal/randSelBitrange + addIfcInst/addUnitInst/… + modif*; CslGenUnitInst.randSelUnit;
    CslGenPort.randSelBitrange; CslGenSignal.randSelBitrange/randSelSignal;
    CslGenPort.copySignal/copyPort/buildSet, CslGenSignal/CslGenBitrange.buildSet; randString() no-arg →
    CslGenRandString.randString(); CSLbase::m_out →
    CslGenCslBase.printSink / runWithPrintSink (ThreadLocal StringBuilder); cGenIsa_ao.h/cpp → CslGenIsaAoTables,
    CslGenIsaInstrFormat (getIsaInstrFormatUsedAt + m_* Text + m_*F flags, EFlags = public int constants),
    CslGenIsaInstr (getIsaInstrUsedAt, getInst = m_inst, m_* accessors), CslGenIsa (getIsaUsedAt + m_* Text + m_generateDecoderF),
    CslGenDesign.addIsaInstrFormat / addIsaInstr / addIsa
  cslc-csl-gen: cGen_Support.h (include stack) + decl/cmd TB harness → com.fastpath.cslc.cslgen.cgentb.CGenTbEslClass
    (regen: scripts/gen_cgen_tb_esl_class.py + Maven validate --check; stems from cslGenericName), CGenTbClassRef, CGenTbError,
    CGenTbTestGen,
    CGenTbChapter, CGenTbParamDecl, CGenTbParamType, CGenTbCmdType (regen: scripts/gen_cgen_tb_cmd_type.py; Maven validate runs
    --check), CGenTbCmdRef,
    CGenDeclTb / CGenCmdTb / CGenInstCmdTb / CGenInstDeclTb (trace + mains + …AndDirs / …AndReport; inst drivers call DeclEmit/CmdEmit for TG_INSTANCE_*),
    CGenTbMainArgs, CGenTbRepositoryPath, CGenTbChapterDirs (executeClean purge + buildMakeDirs + regression_control leaves),
    CGenTbBuildContainers + CGenTbContainers (generic/cmd lookup maps), CGenTbRunContext (ThreadLocal TB globals during f()),
    CGenTbClassSpec + CGenTbSpecs (decl/cmd specs: LANG BITRANGE/FIELD/ENUM/EVENT/MULTI_DIM_BITRANGE, FIFO, INTERCON, MEM/MEM_MAP/REG/REG_FILE/ISA/TESTBENCH/VERIFC state_data+vector; FIELD/UNIT/ISA_FIELD BITRANGE predecl; SIGNAL_GROUP mandatory inner signal),
    CGenTbPreDeclOrder (createPreDeclVectOrder cartesian),
    CGenTbDeclEmit (writes legal decl files), CGenTbCmdEmit (writes legal cmd-call files),
    CGenTbFileHeader, CGenTbGeneratedFile (closeFile trailer + counter), CGenTbStubBuild (driver marker), CGenTbTestCounter,
    CGenTbRegressionCtrl, CGenTbRunStub (containers + purge + layout + context + report), CGenTbReport;
    cGen_Declarations.h enums;
    csl_enum_field.cpp alternate enum/field
    path superseded for Java by interconnect CslGenEnum/CslGenField + CslGenEnumFieldTraceTest
  cslc-csl-gen: cslGen_TB.h/cpp (namespace NSCslGen — distinct mini-model) → com.fastpath.cslc.cslgen.tb.CslGenTbConsts,
    CslGenTbBitrange, CslGenTbSignal, CslGenTbPort, CslGenTbInstance, CslGenTbInterface, CslGenTbUnitDecl/Init/Unit,
    CslGenTbGenerator (addPort, newNameIsValid includes ports; bitrange lower/upper; signal copy branch), CslGenTbMain
  cslc-fastpath: refcounted.h/cpp → RefCounted + RefCounting; fploptions → FplOptions; main.cpp → FastPathMain stub
  cslc-cdom: fpl_module_class.h/cpp → FplModule (abstract, no SystemC); CDOM_Checker.cpp path-prefix / undef slice →
    CdomCheckerState
  cslc-driver: Adapter_Driver.cpp → AdapterDriverMain; CDOM_VerilogGeneratorDriver.cpp →
    CdomVerilogGeneratorDriverMain; SD_Driver.cpp → SdDriverMain; CSIM_Driver.cpp → CsimDriverMain
  cslc-aa: src/aa/wincslc/main/main.cpp → WincslcMain
