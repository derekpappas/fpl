//oanab
//26 may 2008

csl_interface ifc {
  csl_port x(input);
  ifc () {}
};

csl_unit a {
  csl_port clk(input);
  ifc ifc0;
  a () {
    clk.set_attr(clock);
    ifc0.set_clock(clk);
    clk = ifc0.x;
  }
};
