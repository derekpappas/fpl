//oanab
//3 nov 2007
//the next address is bigger than the range

csl_fifo f{
  f() {
    set_width(32);
    set_depth(8);
  }
}; 

csl_unit u{
f f1;
u(){}
};

csl_memory_map_page mpag{
  mpag(){
     add_address_range(125,321);
     set_unit_name(u);
     set_next_address(322);//this address should be between 125 and 321
    }
};

csl_memory_map mmap{
   mmap(){
      set_data_word_width(32);
      set_type(hierarchical);
}
};
