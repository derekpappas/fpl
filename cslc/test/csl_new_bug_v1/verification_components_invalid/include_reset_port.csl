//Monica
//13 november 2007
//invalid test --- include_only a reset port for a vector


csl_unit dut{
    csl_port stim_d(input),stim_v(input);
    csl_port exp_v(output),exp_d(output);
    csl_port reset(input);
    csl_port clk(input);
  dut(){
    clk.set_attr(clock);
    reset.set_attr(reset);
  }
};
csl_vector stim{
  stim(){
    set_unit_name(dut);
    set_direction(input);
    include_only(reset);    //should give an error
    
  }
};
csl_vector exp{
  exp(){
    set_unit_name(dut);
    set_direction(output);
  }
};
csl_testbench tb{
  csl_signal clk(reg);
  dut dut;
  tb(){
    clk.set_attr(clock);
    add_logic(clock, clk, 10, ns);
  }
};
