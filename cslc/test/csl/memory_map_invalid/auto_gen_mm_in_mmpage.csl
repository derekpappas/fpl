//illegal: auto_gen_memory_map() should not work in mmpage.
//andreim
//15 June 2007

csl_memory_map_page mmp_page{
	mmp_page(){
		add_address_range(0,31);
		set_endianess(big_endian);
		set_data_word_width(16);
		set_alignment(4);           // increment = 16 / 4 = 4 
		auto_gen_memory_map();
 	}
};

csl_memory_map mm{
		mm(){
		set_type(flat);
	  set_data_word_width(128);
		}
	};