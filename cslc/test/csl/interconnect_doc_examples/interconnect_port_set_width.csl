//danielt
//november 07,2007

csl_unit a{
  csl_port x(output);
  a(){
    x.set_width(16);}
};
csl_unit b{
  csl_port y(input);
  b(){
    y.set_width(16);}
};
csl_unit top{
  csl_signal data(wire,16);
  a a( .x(data));
  b b( .y(data));
  top(){}};
