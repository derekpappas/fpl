csl_memory_map_page mpage{
  mpage(){
    set_access_rights(SWR,access_read);
  }};
csl_memory_map mmap{
  mpage mpage;
        mmap(){
                set_data_word_width(32);
                //       set_access_rights(SWR,access_read_write);
  }
};
