// ILLEGAL TEST - because is called twice set_field_position for the same
// field_instance, but at a different position 

csl_field fa{
  fa(){
    set_width(3);
  }
};

csl_field f1{
  fa fa_1;
  f1(){
   set_width(32);
   set_field_position(fa_1, 29);
   set_field_position(fa_1, 0);// an error should appear here
  }
};