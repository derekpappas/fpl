
csl_field op {
  op() {
    set_width(2);
  }
};


csl_field instr_fmt {
  op op0;
  op op1;
  op op2;
  op op3;

  instr_fmt() {
    set_width(32);
    set_field_position(op0,30);
    set_next(op0, op1);
    set_next(op1, op2);
    set_next(op2, op3);

  }
};

csl_unit top {
};
