//isa_field_obj_name.get_mnemonic();

csl_isa_field f_op{ 
  f_op(){ 
    set_width(2); 
    set_type(opcode); 
    set_mnemonic("alu"); 
  }
};
csl_isa_field f_op2{ 
  f_op2(){ 
    set_width(3); 
    set_type(opcode); 
    set_mnemonic(f_op.get_mnemonic()); 
  }
};
