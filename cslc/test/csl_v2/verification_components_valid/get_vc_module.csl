//Monicah
//17 oct 2007

csl_unit dut{
  csl_port stim_p(input), exp_p(output);
  dut(){}
};

csl_vector stim{
  stim(){
    set_unit_name(dut);
    set_direction(input);
  }
};


csl_vector exp{
  exp(){
    set_unit_name(stim.get_vc_module());
     set_direction(output);
  }
};
