csl_enum vu {
	sn,
	uj,
	cj,
	qj,
	yp,
	og
};
csl_enum cm {
	gr,
	ot,
	fu,
	xc,
	td,
	bi,
	km,
	lh,
	kf,
	wh
};
csl_enum qc {
	ri = 12,
	lq = 64,
	xd = 10,
	ac = 96,
	pg = 79,
	tg = 11,
	bb = 25,
	db = 71,
	ew = 75,
	ef = 4,
	fo = 53,
	tt = 64
};
csl_isa_instruction_format ju{
    ju( ){
     generate_decoder( wbcm);
  }
}
;
csl_isa_instruction dc : ju{
    dc( ){
    set_asm_mnemonic( ar);
  }
}
;
csl_isa_instruction wm : ju{
    wm( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa_instruction yw : ju{
    yw( ){
    set_asm_mnemonic( csl_list "vi", "rk");
  }
}
;
csl_isa_instruction fj : ju{
    fj( ){
    set_asm_mnemonic( po);
  }
}
;
csl_isa_instruction co : ju{
    co( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa qt{
  co ia;
  yw iu;
  fj bh;
  dc fv;
  wm xf;
  yw ih;
  fj fr;
  co ht;
  wm rx;
  yw jg;
  fj va;
  dc gh;
  wm em;
  dc wd;
  wm nm;
  co mu;
  wm uf;
  yw mb;
  co al;
  wm ps;
  co kb;
  dc if;
  dc si;
  dc xo;
  co jh;
  dc do;
  wm pp;
  fj wa;
  wm jm;
  fj qx;
  co ax;
  dc kc;
  yw st;
  fj ag;
  co ol;
  yw dg;
  dc ul;
  wm yc;
  fj wf;
  wm bj;
  fj dd;
  fj yx;
  co ee;
  co vb;
  wm ip;
  fj tq;
  co qu;
  dc nj;
  wm ej;
  co gg;
  fj yy;
  co pv;
    qt( ){
     set_decoder_out_name_prefix( "im");
     set_decoder_out_name_suffix( "hh");
     generate_decoder( wbqocmfd);
     print( isa.txt);
  }
}
;
csl_fifo om{
   om( ){
     set_physical_implementation( SRAM);
     add_logic( wr_release);
     add_logic( almost_empty, 9);
     add_logic( rd_credit);
  }
}
;
csl_memory_map_page wd{
    wd( ){
     set_address_increment( 9);
     set_next_address( 9);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page ds{
  wd qc;
    ds( ){
     add_address_range( 4, 7);
     set_address_increment( 7);
     set_next_address( 2);
     get_next_address( );
     add( wd);
     set_data_word_width( 0);
     set_aligment( 6);
     get_aligment( );
     get_endianess( );
  }
}
;
csl_memory_map_page xd{
    xd( ){
     add_address_range( 1, 7);
     get_address_increment( );
     set_access_rights( access_read_write, none);
     add_reserved_address_range( 1, 3);
     get_lower_bound( );
     get_upper_bound( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map qj{
  wd po;
  ds qa;
  xd px;
  wd lh;
  xd lo;
    qj( ){
     auto_gen_memory_map( );
     set_type( flat);
     set_data_word_width( 5);
     get_data_word_width( );
     set_prefix( gm);
     get_prefix( );
     set_suffix( ap);
  }
}
;
csl_register ql{
    ql( ){
     add_logic( wr_en);
     set_lock_enable_bit( 0);
     add_logic( dec_signal);
  }
}
;
csl_register mx{
    mx( ){
     set_width( 0);
     set_depth( 1);
     add_logic( read_valid);
     add_logic( bypass);
  }
}
;
