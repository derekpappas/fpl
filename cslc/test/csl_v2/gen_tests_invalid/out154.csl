csl_enum jx {
	lr,
	ds,
	rj
};
csl_enum rm {
	ji = 95,
	vt = 30,
	cs = 31,
	to = 41,
	ab = 59,
	ic = 42,
	bi = 74,
	xq = 72
};
csl_enum ww {
	tn,
	cd,
	mn,
	wr,
	ip
};
csl_isa_instruction_format fc{
    fc( ){
  }
}
;
csl_isa_instruction ju : fc{
    ju( ){
    set_asm_mnemonic( hh);
  }
}
;
csl_isa yl{
  ju fa;
  ju bs;
  ju tu;
  ju kc;
  ju ct;
  ju vm;
  ju bq;
  ju ny;
  ju wt;
    yl( ){
     set_decoder_name( "oy");
     set_decoder_out_name_prefix( "hw");
     set_decoder_out_name_suffix( "dn");
     generate_decoder( wddmsf);
     print( isa.txt);
  }
}
;
csl_fifo nn{
   nn( ){
     add_logic( programable_depth, 6);
     add_logic( width_extend, 8);
  }
}
;
csl_memory_map_page yr{
    yr( ){
     set_address_increment( 4);
     get_next_address( );
     add_reserved_address_range( 1, 9);
     get_lower_bound( );
     get_data_word_wodth( );
     get_aligment( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page no{
    no( ){
     add_address_range( 1, 7);
     set_address_increment( 2);
     get_address_increment( );
     set_access_rights( access_write, access_read);
     get_lower_bound( );
     get_data_word_wodth( );
     set_aligment( 9);
     set_endianess( little_endian);
     get_endianess( );
  }
}
;
csl_memory_map_page en{
  no an;
  yr wc;
    en( ){
     add_address_range( 0, 1);
     get_next_address( );
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map rh{
    rh( ){
     auto_gen_memory_map( );
     set_top_unit( wdbh);
     set_type( page);
     set_data_word_width( 3);
     get_data_word_width( );
     set_prefix( uu);
     get_prefix( );
     set_suffix( tv);
  }
}
;
csl_register ma{
    ma( ){
     set_range( {[5:9], [4:3], [3:7]});
     set_atribute( write);
     constant( 9);
     add_logic( neg_output);
     set_value( 3);
     reset_value( 7);
     clear_value( 7);
     set_count_direction( 1);
     set_start_value( 7);
     add_logic( cnt_dir_signal);
     get_cnt_dir_signal( );
     add_logic( dec_signal);
  }
}
;
csl_register hh{
    hh( ){
     set_width( 1);
     set_depth( 0);
     get_depth( );
  }
}
;
