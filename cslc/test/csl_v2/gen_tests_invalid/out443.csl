csl_enum yy {
	kj,
	kw,
	cv,
	lg,
	au,
	ro,
	eh,
	px,
	fi,
	yv
};
csl_enum ir {
	kk = 33,
	qk = 66,
	ya = 68,
	rx = 51,
	be = 9,
	hn = 57,
	cm = 30,
	kn = 49,
	um = 97,
	lo = 97,
	bp = 4
};
csl_enum vq {
	pe,
	br,
	ak,
	jm,
	wc,
	xv,
	er,
	tg,
	ve,
	vx,
	qg
};
csl_isa_instruction_format ht{
    ht( ){
     set_width( 2);
     generate_decoder( xx);
  }
}
;
csl_isa ug{
    ug( ){
     set_decoder_name( "mp");
     set_decoder_out_name_prefix( "hj");
     print( isa.txt);
  }
}
;
csl_fifo sq{
   sq( ){
  }
}
;
csl_memory_map_page nj{
    nj( ){
     set_address_increment( 2);
     get_address_increment( );
     set_next_address( 6);
     add_reserved_address_range( 9, 6);
     add( sq, "jr", 5);
     set_data_word_width( 6);
     set_aligment( 6);
     set_endianess( little_endian);
     get_endianess( );
  }
}
;
csl_memory_map_page rd{
    rd( ){
     set_next_address( 7);
     set_access_rights( access_read, none);
     get_lower_bound( );
     set_data_word_width( 0);
     set_aligment( 8);
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page lw{
  nj jd;
  nj ek;
    lw( ){
     add_address_range( 2, 6);
     get_address_increment( );
     set_next_address( 9);
     get_next_address( );
     add( sq, "qr", 3);
     get_upper_bound( );
     set_data_word_width( 3);
     get_data_word_wodth( );
     set_aligment( 6);
     get_aligment( );
     set_endianess( big_endian);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map ef{
    ef( ){
     set_top_unit( xxpe);
     set_prefix( mc);
     get_prefix( );
     set_suffix( wc);
  }
}
;
csl_register ws{
    ws( ){
     set_range( {[1:1], [6:6], [5:3]});
     constant( 8);
     add_logic( wr_en);
  }
}
;
csl_register kk{
    kk( ){
     set_width( 3);
     get_width( );
     set_depth( 3);
     create_rtl_module( );
     add_logic( bypass);
  }
}
;
