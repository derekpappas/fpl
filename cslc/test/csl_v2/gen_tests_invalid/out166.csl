csl_enum nb {
	pl = 62,
	gr = 14,
	pj = 94
};
csl_enum ig {
	nj = 51,
	ij = 89
};
csl_enum yr {
	rc = 15,
	nu = 7,
	xu = 83,
	wq = 26,
	ab = 65,
	ig = 40,
	no = 10,
	jb = 6
};
csl_isa_instruction_format kx{
    kx( ){
     generate_decoder( ledhkq);
  }
}
;
csl_isa ti{
    ti( ){
     set_decoder_name( "gj");
     set_decoder_out_name_prefix( "lg");
     set_decoder_out_name_suffix( "sj");
     generate_decoder( dhwqkq);
     print( isa.txt);
  }
}
;
csl_fifo qu{
   qu( ){
     add_logic( async_fifo);
     add_logic( kf);
     add_logic( almost_full, 1);
  }
}
;
csl_memory_map_page bm{
    bm( ){
     get_address_increment( );
     get_next_address( );
     add_reserved_address_range( 9, 1);
     get_lower_bound( );
     set_data_word_width( 6);
     get_data_word_wodth( );
     set_aligment( 2);
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page xj{
  bm tv;
    xj( ){
     get_address_increment( );
     set_next_address( 2);
     add( qu, "on", 9);
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     get_aligment( );
     set_endianess( big_endianlittle_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page uy{
    uy( ){
     get_next_address( );
     add( qu, "ta");
     get_lower_bound( );
     set_aligment( 2);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map cb{
    cb( ){
     auto_gen_memory_map( );
     set_suffix( sr);
     get_suffix( );
  }
}
;
csl_register pk{
    pk( ){
     set_atribute( write);
     constant( 7);
     add_logic( rd_en);
     set_value( 2);
     clear_value( 6);
  }
}
;
csl_register cd{
    cd( ){
     set_width( 0);
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 6);
  }
}
;
