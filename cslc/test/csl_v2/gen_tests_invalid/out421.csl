csl_enum ah {
	vb,
	lx,
	bn,
	wf,
	yp
};
csl_enum co {
	oj = 54,
	ir = 59,
	fi = 48,
	wr = 86,
	lw = 46,
	ly = 63,
	lv = 93
};
csl_enum ra {
	ys = 16,
	ix = 36,
	vt = 6,
	vw = 48,
	ku = 31,
	ej = 11,
	mi = 28,
	xc = 56,
	ri = 10,
	vy = 1,
	sv = 32,
	re = 68
};
csl_isa_instruction_format dy{
    dy( ){
     set_width( 5);
     generate_decoder( fvcy);
  }
}
;
csl_isa me{
    me( ){
     set_decoder_name( "ly");
     set_decoder_out_name_prefix( "tk");
     set_decoder_out_name_suffix( "od");
     generate_decoder( vusi);
     print( isa.txt);
  }
}
;
csl_fifo gw{
   gw( ){
     add_logic( depth_extend, 2);
     add_logic( almost_empty, 5);
  }
}
;
csl_memory_map_page lv{
    lv( ){
     set_address_increment( 1);
     set_next_address( 2);
     add_reserved_address_range( 8, 3);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 2);
     set_aligment( 9);
     get_endianess( );
  }
}
;
csl_memory_map_page hv{
    hv( ){
     add_address_range( 1, 2);
     set_address_increment( 3);
     get_address_increment( );
     get_next_address( );
     set_access_rights( access_read, access_read_write);
     add_reserved_address_range( 5, 5);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 8);
     set_aligment( 2);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page fd{
  lv qq;
  hv xo;
  lv as;
  hv xu;
    fd( ){
     add_address_range( 7, 5);
     set_address_increment( 5);
     get_address_increment( );
     add_reserved_address_range( 7, 4);
     add( gw, "db", );
     add( hv);
     get_lower_bound( );
     get_data_word_wodth( );
     set_aligment( 0);
     get_endianess( );
  }
}
;
csl_memory_map yk{
  lv ub;
  hv ie;
  fd xw;
  fd ge;
    yk( ){
     auto_gen_memory_map( );
lvhv.add_to_memory_map( access_write); 
     set_type( flat);
lvhv.set_access_rights_enum( lvhv); 
     set_data_word_width( 1);
  }
}
;
csl_register gq{
    gq( ){
     set_range( {[5:4], [6:0], [9:1]});
     set_width( 4);
     set_address( yk, );
     add_logic( rd_en);
     set_end_value( 3);
  }
}
;
csl_register sn{
    sn( ){
     set_width( 1);
  }
}
;
