csl_enum ig {
	cw = 78,
	pu = 65,
	gs = 14,
	mm = 26,
	pc = 58,
	px = 82,
	db = 43,
	wy = 88,
	qv = 78
};
csl_enum gm {
	yh = 31,
	nv = 94,
	dm = 93,
	ew = 27,
	to = 8,
	fv = 73
};
csl_enum mx {
	qu = 76,
	qf = 58,
	ih = 40,
	jx = 74,
	ho = 14,
	yf = 14,
	np = 28,
	dv = 30,
	ef = 28,
	ft = 41,
	di = 18
};
csl_isa_instruction_format tr{
    tr( ){
     set_width( 9);
     generate_decoder( tj);
  }
}
;
csl_isa_instruction mn : tr{
    mn( ){
    set_asm_mnemonic( ae);
  }
}
;
csl_fifo rx{
   rx( ){
     set_dimension( 2, 1);
     add_logic( wr_hold, 1);
     add_logic( parallel_output, all);
     add_logic( almost_full, 0);
  }
}
;
csl_memory_map_page ix{
    ix( ){
     add_address_range( 5, 5);
     set_next_address( 5);
     get_next_address( );
     add_reserved_address_range( 0, 4);
     get_lower_bound( );
     get_upper_bound( );
     get_data_word_wodth( );
     set_aligment( 3);
     get_aligment( );
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page il{
    il( ){
     set_address_increment( 3);
     set_endianess( little_endian);
  }
}
;
csl_memory_map_page rb{
    rb( ){
     add_address_range( 3, 6);
     set_address_increment( 7);
     set_next_address( 6);
     set_access_rights( none, none);
     add_reserved_address_range( 3, 5);
     add( ixil);
     get_lower_bound( );
     get_upper_bound( );
     set_endianess( little_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map rs{
    rs( ){
ilrb.set_access_rights_enum( ixilrb); 
  }
}
;
csl_register gc{
    gc( ){
     constant( 3);
     stop_at_end_value( );
  }
}
;
csl_register uo{
    uo( ){
     set_width( 5);
     set_depth( 0);
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 4);
  }
}
;
