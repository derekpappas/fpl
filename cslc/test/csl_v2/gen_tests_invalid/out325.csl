csl_enum ii {
	mq,
	oo,
	nj,
	we,
	ma,
	jy,
	vw,
	kj,
	uo,
	jw,
	cv
};
csl_enum po {
	ng = 44,
	po = 25,
	py = 87,
	wc = 61,
	rl = 89,
	tg = 47,
	uq = 43
};
csl_enum kj {
	tw = 69,
	ip = 11,
	vt = 33,
	yq = 36,
	kj = 85,
	nn = 58
};
csl_isa_instruction_format hd{
    hd( ){
     set_width( 3);
     generate_decoder( wf);
  }
}
;
csl_isa le{
    le( ){
     set_decoder_name( "gh");
     set_decoder_out_name_prefix( "bs");
     generate_decoder( wflr);
  }
}
;
csl_fifo ks{
   ks( ){
     add_logic( stall_wr_side);
     add_logic( flow);
  }
}
;
csl_memory_map_page hy{
    hy( ){
     set_address_increment( 9);
     get_address_increment( );
     get_next_address( );
     set_access_rights( none, none);
     add_reserved_address_range( 6, 7);
     get_upper_bound( );
     set_data_word_width( 9);
     set_aligment( 7);
     get_endianess( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page qk{
  hy iy;
    qk( ){
     add_address_range( 7, 6);
     set_address_increment( 7);
     get_next_address( );
     set_access_rights( access_read, access_read);
     set_data_word_width( 1);
     get_data_word_wodth( );
     set_aligment( 0);
     get_aligment( );
     set_endianess( big_endianbig_endian);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page kh{
  hy ie;
  qk rp;
    kh( ){
     set_next_address( 6);
     get_next_address( );
     add_reserved_address_range( 9, 5);
     add( hyqk);
     set_aligment( 7);
  }
}
;
csl_memory_map er{
  hy wt;
  qk ng;
  hy uo;
  qk qf;
    er( ){
hy.add_to_memory_map( 5,access_read); 
     set_type( page);
     set_prefix( sk);
     get_prefix( );
     set_suffix( aa);
  }
}
;
csl_register ld{
    ld( ){
     set_type( STATUS);
  }
}
;
csl_register fo{
    fo( ){
     set_width( 9);
     get_width( );
     set_depth( 6);
     add_logic( read_valid);
     create_rtl_module( );
  }
}
;
