csl_enum ph {
	xj = 76,
	hi = 99
};
csl_enum vd {
	vp,
	li,
	do,
	eb,
	ne,
	uf,
	yi,
	ax,
	gr,
	ch
};
csl_enum du {
	lu,
	ek,
	em,
	uc,
	rt
};
csl_isa_instruction_format dt{
    dt( ){
     set_width( 9);
  }
}
;
csl_isa rt{
    rt( ){
     set_decoder_name( "ti");
  }
}
;
csl_fifo me{
   me( ){
     add_logic( credit);
     add_logic( rd_credit);
  }
}
;
csl_memory_map_page lp{
    lp( ){
     set_access_rights( access_read, access_read);
     get_upper_bound( );
     get_data_word_wodth( );
     set_aligment( 0);
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page wh{
    wh( ){
     add_address_range( 4, 3);
     set_next_address( 9);
     get_next_address( );
     add_reserved_address_range( 6, 1);
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map_page mk{
  wh cx;
    mk( ){
     add_address_range( 1, 4);
     get_address_increment( );
     get_next_address( );
     get_lower_bound( );
     get_upper_bound( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map xo{
  lp fq;
  wh da;
  mk ep;
  wh oo;
  lp jk;
  wh rs;
    xo( ){
     set_type( flat);
lp.set_access_rights_enum( lp); 
  }
}
;
csl_register bw{
    bw( ){
     set_width( 2);
     get_width( );
     set_depth( 6);
     create_rtl_module( );
     set_const_value( 4);
     add_logic( bypass);
  }
}
;
