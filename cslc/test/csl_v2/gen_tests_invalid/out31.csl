csl_enum dk {
	gg = 87,
	we = 25,
	lm = 71,
	yg = 60
};
csl_enum wp {
	rw = 75,
	oy = 65,
	oy = 39,
	fi = 23,
	ga = 63,
	xw = 78,
	fv = 74,
	gi = 80
};
csl_enum sa {
	iy = 17,
	ko = 63,
	fk = 18,
	us = 83,
	yt = 81,
	av = 63,
	mr = 0,
	ud = 56,
	sk = 41
};
csl_isa_instruction_format kf{
    kf( ){
     set_width( 9);
     generate_decoder( xubm);
  }
}
;
csl_isa yn{
    yn( ){
     set_decoder_name( "mi");
     set_decoder_out_name_prefix( "yh");
     print( isa.txt);
  }
}
;
csl_fifo xn{
   xn( ){
     add_logic( programable_depth, 1);
     add_logic( stall);
     add_logic( wr_credit);
  }
}
;
csl_memory_map_page jj{
    jj( ){
     get_address_increment( );
     set_next_address( 2);
     get_next_address( );
     add( xn, "eh");
     get_lower_bound( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page vn{
    vn( ){
     add_address_range( 2, 3);
     get_address_increment( );
     get_next_address( );
     set_access_rights( access_read, access_read);
     add_reserved_address_range( 3, 2);
     get_lower_bound( );
     set_data_word_width( 5);
     get_data_word_wodth( );
     get_aligment( );
     get_endianess( );
  }
}
;
csl_memory_map_page qs{
  jj wr;
  vn xg;
  jj df;
  vn ae;
  jj qr;
  vn ln;
    qs( ){
     add_address_range( 3, 8);
     set_address_increment( 3);
     get_address_increment( );
     set_next_address( 2);
     add_reserved_address_range( 6, 5);
     set_data_word_width( 1);
     set_symbol_max_lenght( );
  }
}
;
csl_memory_map sc{
    sc( ){
     set_top_unit( xubmrd);
vn.set_access_rights_enum( jjqs); 
     set_data_word_width( 4);
     get_data_word_width( );
  }
}
;
csl_register mr{
    mr( ){
     set_range( {[2:2], [3:1], [2:4], [0:6], [6:4], [0:8]});
     set_type( CTL);
     add_logic( serial_output);
     init_value( 0);
     reset_value( 9);
     set_count_amount( 0);
     set_count_direction( 4);
     set_start_value( 4);
     add_logic( cnt_dir_signal);
  }
}
;
csl_register rs{
    rs( ){
     set_width( 8);
     get_width( );
     create_rtl_module( );
     set_const_value( 4);
  }
}
;
