csl_enum ko {
	yf = 49,
	cj = 77,
	es = 49,
	ox = 20,
	yk = 67,
	se = 85,
	dn = 15,
	hn = 46,
	ly = 65,
	yl = 65,
	rk = 65
};
csl_enum ib {
	uy = 69,
	nf = 13,
	ba = 12,
	tb = 70,
	gx = 1,
	mm = 45,
	qb = 51
};
csl_enum ms {
	mg = 44,
	yr = 13,
	vj = 67,
	fh = 73,
	ig = 55
};
csl_isa wb{
    wb( ){
     set_decoder_name( "hp");
     set_decoder_out_name_prefix( "eq");
     set_decoder_out_name_suffix( "fs");
     generate_decoder( lvlx);
     print( isa.txt);
  }
}
;
csl_fifo tx{
   tx( ){
     add_logic( flow);
  }
}
;
csl_memory_map_page jk{
    jk( ){
     set_address_increment( 5);
     get_address_increment( );
     set_next_address( 9);
     set_access_rights( access_read, access_read);
     add_reserved_address_range( 9, 6);
     get_lower_bound( );
     set_data_word_width( 5);
     set_aligment( 5);
     get_aligment( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page yi{
    yi( ){
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page lk{
    lk( ){
     add_address_range( 3, 6);
     set_next_address( 5);
     add_reserved_address_range( 9, 7);
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 2);
     get_data_word_wodth( );
     set_aligment( 2);
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map fx{
  lk xy;
  jk bv;
  yi yo;
  lk uo;
    fx( ){
.add_to_memory_map( 6,access_read_write); 
     set_type( page);
     set_data_word_width( 7);
     get_data_word_width( );
     set_suffix( ed);
  }
}
;
csl_register wr{
    wr( ){
     set_type( CNT);
     set_count_amount( 6);
     set_count_direction( 3);
     get_cnt_dir_signal( );
  }
}
;
csl_register sf{
    sf( ){
     set_const_value( 2);
  }
}
;
