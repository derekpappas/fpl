csl_enum oe {
	ft,
	vh,
	il,
	ot
};
csl_enum sj {
	ln = 93,
	mn = 90,
	qv = 47,
	qo = 72,
	vx = 64,
	ul = 50,
	nl = 95,
	hl = 6,
	jt = 7,
	nb = 97,
	xu = 52,
	ut = 97,
	cd = 34,
	hr = 64,
	ya = 76,
	bn = 44,
	pp = 80
};
csl_enum dh {
	lt,
	xk,
	ts,
	vk,
	hf
};
csl_isa_instruction_format dq{
    dq( ){
  }
}
;
csl_isa_instruction jc : dq{
    jc( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa_instruction ch : dq{
    ch( ){
    set_asm_mnemonic( );
  }
}
;
csl_isa sb{
  ch lq;
  jc hu;
  ch bk;
  jc jf;
  ch wk;
  ch xc;
  jc ua;
  jc sq;
  jc cc;
  ch uo;
  jc ex;
  jc hj;
  jc fv;
  ch hy;
  jc oc;
  ch lb;
  ch fx;
  jc ha;
  jc ku;
  jc xg;
  ch ep;
  ch yd;
  jc bh;
  ch cp;
  jc od;
    sb( ){
     set_decoder_name( "ws");
     set_decoder_out_name_prefix( "iy");
     set_decoder_out_name_suffix( "vh");
     generate_decoder( hkrq);
     print( isa.txt);
  }
}
;
csl_fifo nt{
   nt( ){
     add_logic( stall_rd_side);
     add_logic( almost_empty, 0);
  }
}
;
csl_memory_map_page hm{
    hm( ){
     add_address_range( 7, 6);
     set_address_increment( 5);
     get_next_address( );
     add_reserved_address_range( 2, 4);
     get_lower_bound( );
     set_aligment( 0);
     get_aligment( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page jx{
  hm xp;
    jx( ){
     set_next_address( 1);
     set_access_rights( access_read_write, none);
     add( hm);
     get_data_word_wodth( );
     set_endianess( little_endian);
  }
}
;
csl_memory_map nk{
    nk( ){
     set_top_unit( na);
     set_prefix( fw);
     get_prefix( );
     set_suffix( md);
  }
}
;
csl_register fa{
    fa( ){
     set_range( [7:3]);
     set_type( SEMA);
     add_logic( neg_output);
     set_count_amount( 7);
     set_end_value( 8);
  }
}
;
csl_register bi{
    bi( ){
     add_logic( bypass);
  }
}
;
