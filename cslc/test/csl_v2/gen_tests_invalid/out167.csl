csl_enum ij {
	rm = 87,
	xl = 3,
	kl = 28,
	nt = 10,
	km = 25,
	jd = 76,
	uy = 19,
	cd = 87,
	an = 99,
	ay = 37,
	pd = 21,
	hr = 40,
	wv = 44,
	ks = 4
};
csl_enum hs {
	fy = 98,
	ff = 21,
	ks = 28,
	qx = 7,
	oe = 86,
	ka = 31,
	ji = 48,
	yl = 84,
	ny = 87,
	cn = 6,
	ar = 24,
	ti = 68,
	lq = 32,
	gv = 93,
	ln = 64,
	rq = 79,
	my = 13,
	yw = 58,
	rp = 43,
	wi = 71
};
csl_enum vk {
	ig,
	uh,
	ti,
	iv,
	xj,
	qr,
	te,
	ka,
	dy,
	nu,
	ra,
	tq,
	yo,
	jx
};
csl_isa_instruction_format vo{
    vo( ){
     set_width( 7);
     generate_decoder( wdqt);
  }
}
;
csl_isa fa{
    fa( ){
     set_decoder_name( "fu");
     set_decoder_out_name_prefix( "ww");
     set_decoder_out_name_suffix( "qh");
     generate_decoder( viwx);
     print( isa.txt);
  }
}
;
csl_fifo yo{
   yo( ){
     set_physical_implementation( SRAM);
     add_logic( depth_extend, 0);
     add_logic( rd_words, 8);
     add_logic( credit);
     add_logic( wr_credit);
  }
}
;
csl_memory_map_page gp{
    gp( ){
     add_address_range( 8, 0);
     set_address_increment( 1);
     get_address_increment( );
     set_next_address( 6);
     get_next_address( );
     set_access_rights( access_write, access_read_write);
     add_reserved_address_range( 4, 0);
     get_lower_bound( );
     get_upper_bound( );
     get_aligment( );
     get_endianess( );
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page pa{
  gp oc;
  gp ik;
  gp gn;
    pa( ){
     set_address_increment( 8);
     get_address_increment( );
     set_next_address( 2);
     get_next_address( );
     add_reserved_address_range( 2, 0);
     add( yo, "ax");
     get_lower_bound( );
     get_upper_bound( );
     set_data_word_width( 3);
     get_data_word_wodth( );
     set_aligment( 9);
     get_aligment( );
     set_endianess( big_endianlittle_endian);
     set_symbol_max_lenght( );
     get_symbol_lenght( );
  }
}
;
csl_memory_map_page wg{
    wg( ){
     add_address_range( 3, 9);
     set_address_increment( 7);
     set_access_rights( none, access_write);
     get_data_word_wodth( );
     get_aligment( );
     set_endianess( little_endian);
     get_symbol_lenght( );
  }
}
;
csl_memory_map lk{
  gp wp;
  wg vg;
  pa km;
  wg hm;
  gp cr;
  wg gy;
    lk( ){
     auto_gen_memory_map( );
     set_type( flat);
wg.set_access_rights_enum( pawg); 
     set_data_word_width( 4);
     get_data_word_width( );
     set_prefix( yc);
     set_suffix( nj);
  }
}
;
csl_register po{
    po( ){
     set_width( 4);
     get_atributes( );
     add_logic( neg_output);
     init_value( 8);
     reset_value( 0);
     clear_value( 5);
     add_logic( cnt_dir_signal);
  }
}
;
csl_register xm{
    xm( ){
     add_logic( read_valid);
     create_rtl_module( );
     set_const_value( 1);
  }
}
;
