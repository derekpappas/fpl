//danielt
//november 07,2007

csl_unit u {
};

csl_memory_map_page mpage_0 {
  mpage_0(){
    add_address_range(0, 63);
    set_unit_name(u);
    set_symbol_max_length(10);
  }
};

csl_memory_map mmap{
  mmap(){
    set_type(hierarchical);
}
};
