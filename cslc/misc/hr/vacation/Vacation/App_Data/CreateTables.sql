CREATE TABLE company (banner VARCHAR(40), name VARCHAR(40), email VARCHAR(40));
CREATE TABLE users (id INTEGER, username VARCHAR(40), password VARCHAR(40), class INTEGER, email VARCHAR(40));
CREATE TABLE hours (id INTEGER, date DATETIME, hours INTEGER, added DATETIME);
